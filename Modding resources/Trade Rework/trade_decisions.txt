trade_category = {
	ic_trade = {
		allowed = {
			tag = UNS
		}
		available = {
			always = no
		}
		activation = {
		}
		selectable_mission = yes
		days_mission_timeout = 30

		timeout_effect = {
			export_script_for_pool = yes
			standard_deviation_for_industrial_base = yes
			every_country = {
				if = {
					limit = {
						OR = {
							check_variable = {
								trade_automation = 2
							}
							is_ai = yes
						}
					}
					country_event = trade.1
					#trade_check = yes
				}
			}
			activate_mission = ic_trade
			#export_script_for_pool = yes
		}
	}
}