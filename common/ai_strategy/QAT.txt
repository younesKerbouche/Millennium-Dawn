#Libya befriendeed Qatar
QAT_befriend_libya = {
	allowed = { original_tag = QAT }

	enable = {
		LBA = { has_completed_focus = LBA_contact_qatar }
		NOT = { has_war_with = LBA }
	}

	abort_when_not_enabled = yes

	ai_strategy = { type = befriend id = "LBA" value = 25 }
}

#Qatar heavily supported Libyan rebels
QAT_support_libyan_rebels = {
	allowed = { original_tag = QAT }

	enable = {
		LBA = { NOT = { has_completed_focus = LBA_contact_qatar } }
	}

	abort_when_not_enabled = yes

	ai_strategy = { type = befriend id = "LRB" value = 100 }
	ai_strategy = { type = support id = "LRB" value = 150 }
}