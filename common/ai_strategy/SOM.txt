SHB_conquer_somaliland = {
	allowed = {
		OR = {
			original_tag = SHB
			original_tag = SOM
		}
	}
	enable = {
		OR = {
			has_government = nationalist
			has_government = fascism
		}
		NOT = { owns_state = 237 }
		NOT = { owns_state = 238 }
	}
	abort_when_not_enabled = yes

	ai_strategy = { type = antagonize id = "SML" value = 100 }
	ai_strategy = { type = conquer id = "SML" value = 100 }
}

SHB_conquer_ogaden = {
	allowed = {
		OR = {
			original_tag = SHB
			original_tag = SOM
		}
	}
	enable = {
		OR = {
			has_government = nationalist
			has_government = fascism
		}
		NOT = { owns_state = 236 }
	}
	abort_when_not_enabled = yes

	ai_strategy = { type = antagonize id = "ETH" value = 100 }
	ai_strategy = { type = conquer id = "ETH" value = 100 }
}

#AMISOM
AMISOM_mission_to_somalia = {
	allowed = {
		OR = {
			original_tag = UGA
			original_tag = BUR
			original_tag = KEN
			original_tag = DJI
			original_tag = SIE
			original_tag = NIG
			original_tag = GAH
		}
	}
	enable = {
		OR = {
			has_government = democratic
			has_government = communism
			has_government = neutrality
		}
		SOM = {
			OR = {
				has_war_with = SHB
				has_civil_war = yes
			}
			OR = {
				has_government = democratic
				has_government = communism
				has_government = neutrality
			}
		}
	}
	abort_when_not_enabled = yes

	ai_strategy = { type = befriend id = "SOM" value = 100 }
	ai_strategy = { type = support id = "SOM" value = 100 }
	ai_strategy = { type = send_volunteers_desire id = "SOM" value = 100 }
	ai_strategy = { type = protect id = "SOM" value = 50 }
	ai_strategy = { type = influence id = "SOM" value = 50 }
}
SHB_halt_front= {
	allowed = {
		original_tag = SHB
	}
	enable = {
		OR = {
			has_country_flag = Indha_Alkabab_mantle_id
			has_country_flag = Ayrow_Alkabab_mantle_id
			has_country_flag = Mansoor_Alkabab_mantle_id
		}
	}
	abort_when_not_enabled = yes

	ai_strategy = {
		type = front_control
		tag = SOM
		execute_order = no
		manual_attack = no
	}
	ai_strategy = {
		type = front_unit_request
		tag = SOM
		value = -1000
	}
	ai_strategy = {
		type = front_control
		tag = SHB
		execute_order = no
		manual_attack = no
	}
	ai_strategy = {
		type = front_unit_request
		tag = SHB
		value = -1000
	}
}
SHB_halt_front= {
	allowed = {
		OR = { 
			original_tag = SNA
			original_tag = SOM
			original_tag = SHB
		}
	}
	enable = {
		has_global_flag = SOM_low_level_Alkabab
	}
	abort_when_not_enabled = yes

	ai_strategy = {
		type = front_control
		tag = SOM
		execute_order = no
		manual_attack = no
	}
	ai_strategy = {
		type = garrison
		value = 1000
	}
	ai_strategy = {
		type = front_unit_request
		tag = SOM
		value = -1000
	}
	ai_strategy = {
		type = front_control
		tag = SHB
		execute_order = no
		manual_attack = no
	}
	ai_strategy = {
		type = front_unit_request
		tag = SHB
		value = -1000
	}
}

