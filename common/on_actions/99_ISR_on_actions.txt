on_actions = {
	on_weekly_ISR = {
		effect = {
			if = {
				limit = {
					check_variable = { ISR_palestine_situation < 19 }
					PAL = { is_subject_of = ISR }
				}
				add_ideas = ISR_calm_security
				remove_ideas = ISR_stable_security
				remove_ideas = ISR_routine_security
				remove_ideas = ISR_frequent_incidents
				remove_ideas = ISR_very_frequent_incidents
				remove_ideas = ISR_intifada
				remove_ideas = palestinian_resistance
			}
			if = {
				limit = {
					check_variable = {
						ISR_palestine_situation > 19
					}
					check_variable = {
						ISR_palestine_situation < 34
					}
					PAL = { is_subject_of = ISR }
				}
				add_ideas = ISR_stable_security
				remove_ideas = ISR_calm_security
				remove_ideas = ISR_routine_security
				remove_ideas = ISR_frequent_incidents
				remove_ideas = ISR_very_frequent_incidents
				remove_ideas = ISR_intifada
				remove_ideas = palestinian_resistance
			}
			if = {
				limit = {
					check_variable = {
						ISR_palestine_situation > 34
					}
					check_variable = {
						ISR_palestine_situation < 49
					}
					PAL = { is_subject_of = ISR }
				}
				add_ideas = ISR_routine_security
				remove_ideas = ISR_calm_security
				remove_ideas = ISR_stable_security
				remove_ideas = ISR_frequent_incidents
				remove_ideas = ISR_very_frequent_incidents
				remove_ideas = ISR_intifada
				remove_ideas = palestinian_resistance
			}
			if = {
				limit = {
					check_variable = {
						ISR_palestine_situation > 49
					}
					check_variable = {
						ISR_palestine_situation < 64
					}
					PAL = { is_subject_of = ISR }
				}
				add_ideas = ISR_frequent_incidents
				remove_ideas = ISR_calm_security
				remove_ideas = ISR_stable_security
				remove_ideas = ISR_routine_security
				remove_ideas = ISR_very_frequent_incidents
				remove_ideas = ISR_intifada
				remove_ideas = palestinian_resistance
			}
			if = {
				limit = {
					check_variable = {
						ISR_palestine_situation > 64
					}
					check_variable = {
						ISR_palestine_situation < 79
					}
					PAL = { is_subject_of = ISR }
				}
				add_ideas = ISR_very_frequent_incidents
				remove_ideas = ISR_calm_security
				remove_ideas = ISR_stable_security
				remove_ideas = ISR_routine_security
				remove_ideas = ISR_frequent_incidents
				remove_ideas = ISR_intifada
				remove_ideas = palestinian_resistance
			}
			if = {
				limit = {
					check_variable = {
						ISR_palestine_situation > 79
					}
					check_variable = {
						ISR_palestine_situation < 94
					}
					PAL = { is_subject_of = ISR }
				}
				add_ideas = ISR_intifada
				remove_ideas = ISR_calm_security
				remove_ideas = ISR_stable_security
				remove_ideas = ISR_routine_security
				remove_ideas = ISR_frequent_incidents
				remove_ideas = ISR_very_frequent_incidents
				remove_ideas = palestinian_resistance
			}
			if = {
				limit = {
					check_variable = {
						ISR_palestine_situation > 94
					}
					PAL = { is_subject_of = ISR }
				}
				add_ideas = palestinian_resistance
				remove_ideas = ISR_calm_security
				remove_ideas = ISR_stable_security
				remove_ideas = ISR_routine_security
				remove_ideas = ISR_frequent_incidents
				remove_ideas = ISR_very_frequent_incidents
				remove_ideas = ISR_intifada
			}
			ISR_update_palestine = yes
		}
	}
	on_monthly_ISR = {
		effect = {
			if = {
				limit = {
					has_country_flag = ISR_bibi_scandals
					has_country_leader = {
						name = "Benjamin Netanyahu"
						ruling_only = yes
					}
				}
				random_list = {
					50 = {
						country_event = israel_likud.1
					}
					50 = {
						country_event = israel_likud.2
					}
				}

			}
			if = {
				limit = {
					check_variable = {
						var = ISR_palestine_situation
						value = 70
						compare = greater_than_or_equals
					}
				}
				subtract_from_variable = {
					ISR_palestine_situation_additional = 5
				}
			}
		}
	}
}
