BRA_historical_plan = {
	allowed = { original_tag = BRA }
	name = "Historical Plan"
	desc = "Behavior for Historical Brazil"

	enable = {
		original_tag = BRA
		is_historical_focus_on = yes
	}

	ai_national_focuses = {
		BRA_cardosos_last_years
		BRA_reinstating_imf_debts
		BRA_a_modern_recession
		BRA_crippled_currency
		BRA_nossa_economia
		BRA_pay_back_the_imf
		BRA_amazonian_development_project
		BRA_logging_reform
		BRA_growth_through_freedom
		BRA_march_of_the_excluded
		BRA_native_protests
		BRA_angry_natives
		BRA_the_workers_party_political_path
		BRA_improve_workers_rights
		BRA_social_programs_agenda_lula
		BRA_fome_zero
	}

	ai_strategy = { type = befriend id = USA value = 30 }
	ai_strategy = { type = befriend id = CAN value = 30 }
	ai_strategy = { type = befriend id = MEX value = 30 }
	ai_strategy = { type = befriend id = CHL value = 30 }
	ai_strategy = { type = befriend id = BOL value = 30 }
	ai_strategy = { type = befriend id = ECU value = 30 }
	ai_strategy = { type = befriend id = PRU value = 30 }
	ai_strategy = { type = befriend id = COL value = 30 }
	ai_strategy = { type = befriend id = GUY value = 30 }
	ai_strategy = { type = befriend id = SUR value = 30 }

	ai_strategy = { type = befriend id = SOV value = 30 }
	ai_strategy = { type = befriend id = CHI value = 30 }
	ai_strategy = { type = befriend id = RAJ value = 30 }

	# Keep small, as it is used as a factor for some things (such as research needs)
	# Recommended around 1.0. Useful for relation between plans
	weight = {
		factor = 1.0
		modifier = {
			factor = 1.0
		}
	}
}

BRA_lula_plan = {
	allowed = { original_tag = BRA }
	name = "Lula Plan"
	desc = "Behavior for Lula Brazil"

	enable = {
		original_tag = BRA
		has_global_flag = BRA_LULA
	}

	ai_national_focuses = {
		BRA_cardosos_last_years
		BRA_reinstating_imf_debts
		BRA_a_modern_recession
		BRA_crippled_currency
		BRA_nossa_economia
		BRA_pay_back_the_imf
		BRA_amazonian_development_project
		BRA_logging_reform
		BRA_growth_through_freedom
		BRA_march_of_the_excluded
		BRA_native_protests
		BRA_angry_natives
		BRA_the_workers_party_political_path
		BRA_improve_workers_rights
		BRA_social_programs_agenda_lula
		BRA_fome_zero
	}

	ai_strategy = { type = befriend id = USA value = 30 }
	ai_strategy = { type = befriend id = CAN value = 30 }
	ai_strategy = { type = befriend id = MEX value = 30 }
	ai_strategy = { type = befriend id = CHL value = 30 }
	ai_strategy = { type = befriend id = BOL value = 30 }
	ai_strategy = { type = befriend id = ECU value = 30 }
	ai_strategy = { type = befriend id = PRU value = 30 }
	ai_strategy = { type = befriend id = COL value = 30 }
	ai_strategy = { type = befriend id = GUY value = 30 }
	ai_strategy = { type = befriend id = SUR value = 30 }

	ai_strategy = { type = befriend id = SOV value = 30 }
	ai_strategy = { type = befriend id = CHI value = 30 }
	ai_strategy = { type = befriend id = RAJ value = 30 }

	# Keep small, as it is used as a factor for some things (such as research needs)
	# Recommended around 1.0. Useful for relation between plans
	weight = {
		factor = 1.0
		modifier = {
			factor = 1.0
		}
	}
}

BRA_serra_plan = {
	allowed = { original_tag = BRA }
	name = "Serra Plan"
	desc = "Behavior for Serra Brazil"

	enable = {
		original_tag = BRA
		has_global_flag = BRA_SERRA
	}

	ai_national_focuses = {
		BRA_cardosos_last_years
		BRA_reinstating_imf_debts
		BRA_a_modern_recession
		BRA_crippled_currency
		BRA_nossa_economia
		BRA_pay_back_the_imf
		BRA_amazonian_development_project
		BRA_logging_reform
		BRA_growth_through_freedom
		BRA_march_of_the_excluded
		BRA_native_protests
		BRA_angry_natives
		BRA_the_workers_party_political_path
		BRA_improve_workers_rights
		BRA_social_programs_agenda_lula
		BRA_fome_zero
	}

	ai_strategy = { type = befriend id = USA value = 30 }
	ai_strategy = { type = befriend id = CAN value = 30 }
	ai_strategy = { type = befriend id = MEX value = 30 }
	ai_strategy = { type = befriend id = CHL value = 30 }
	ai_strategy = { type = befriend id = BOL value = 30 }
	ai_strategy = { type = befriend id = ECU value = 30 }
	ai_strategy = { type = befriend id = PRU value = 30 }
	ai_strategy = { type = befriend id = COL value = 30 }
	ai_strategy = { type = befriend id = GUY value = 30 }
	ai_strategy = { type = befriend id = SUR value = 30 }

	# Keep small, as it is used as a factor for some things (such as research needs)
	# Recommended around 1.0. Useful for relation between plans
	weight = {
		factor = 1.0
		modifier = {
			factor = 1.0
		}
	}
}
