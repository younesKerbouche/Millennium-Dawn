preah_vihear_decisions_category = {
	icon = GFX_decision_category_generic_territorial
	picture = GFX_decision_cat_generic_asia
	priority = 125

	allowed = {
		OR = {
			original_tag = SIA
			original_tag = CBD
		}
	}

	visible = {
		country_exists = SIA
		country_exists = CBD
		NOT = {
			OR = {
				SIA = { is_subject_of = CBD }
				CBD = { is_subject_of = SIA }
				SIA = { is_in_faction_with = CBD }
				has_global_flag = preah_vihear_resolved
				AND = {
					SIA = { is_subject = yes }
					CBD = { is_subject = yes }
					SIA = { OVERLORD = { has_subject = CBD } }
				}
			}
		}
	}
}
