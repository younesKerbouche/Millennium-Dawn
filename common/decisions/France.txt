# Author(s): AngriestBird, Elra
FRA_french_space_decision_category = {
	FRA_run_support_campaigns_short = {
		cost = 75
		icon = political_actions

		days_remove = 45
		days_re_enable = 7
		fire_only_once = no

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision FRA_run_support_campaigns_short"
			set_temp_variable = { temp_change = 2.5 }
			FRA_space_enthusiasm_effect = yes
			set_temp_variable = { treasury_change = gdp_total }
			multiply_temp_Variable = { treasury_change = -0.005 }
			modify_treasury_effect = yes
		}

		ai_will_do = {
			factor = 5

			modifier = {
				add = 0
				is_historical_focus_on = yes
			}
		}
	}

	FRA_run_support_campaigns_long = {
		cost = 75
		icon = political_actions

		days_remove = 90
		days_re_enable = 14
		fire_only_once = no

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision FRA_run_support_campaigns_long"
			set_temp_variable = { treasury_change = gdp_total }
			multiply_temp_Variable = { treasury_change = -0.01 }
			modify_treasury_effect = yes
			set_temp_variable = { temp_change = 5 }
			FRA_space_enthusiasm_effect = yes
		}

		ai_will_do = {
			factor = 5

			modifier = {
				add = 0
				is_historical_focus_on = yes
			}
		}
	}

	FRA_balance_the_cnes_budget = {
		cost = 100
		icon = GFX_decision_Rebalance_CNES_Budget

		days_remove = 45
		fire_only_once = yes

		available = {
			has_completed_focus = FRA_cnes_budgeting
			custom_trigger_tooltip = {
				tooltip = fra_has_enough_space_support_tt
				NOT = { check_variable = { FRA_peoples_support < 35 } }
			}
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision FRA_balance_the_cnes_budget"
			set_temp_variable = { treasury_change = -1 }
			modify_treasury_effect = yes
			set_temp_variable = { temp_change = 5 }
			FRA_space_enthusiasm_effect = yes
			set_temp_variable = { temp_change = 0.250 }
			FRA_space_income_effect = yes
		}
		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove FRA_balance_the_cnes_budget"
			remove_ideas = FRA_idea_cnes_failing
		}

		ai_will_do = {
			factor = 5

			modifier = {
				add = 20
				is_historical_focus_on = yes
			}
			modifier = {
				add = 20
				has_idea = FRA_idea_cnes_failing
			}
		}
	}

	FRA_launch_commercial_satellite = {
		cost = 150
		icon = GFX_decision_Launch_Commercial_Satellite

		days_remove = 120
		days_re_enable = 25

		available = {
			has_completed_focus = FRA_french_space
			custom_trigger_tooltip = {
				tooltip = fra_has_enough_space_support_tt
				NOT = { check_variable = { FRA_peoples_support < 35 } }
			}
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove FRA_launch_commercial_satellite"
			set_temp_variable = { treasury_change = -3 }
			modify_treasury_effect = yes
			set_temp_variable = { temp_change = -5 }
			FRA_space_enthusiasm_effect = yes
			set_temp_variable = { temp_change = 0.350 }
			FRA_space_income_effect = yes
		}
	}

	FRA_prep_international_space_station = {
		cost = 100
		icon = GFX_decision_Prepare_an_ISS_Launch

		days_remove = 30
		days_re_enable = 120

		available = {
			has_completed_focus = FRA_french_space
			NOT = { has_idea = FRA_idea_space_research }
			custom_trigger_tooltip = {
				tooltip = fra_has_enough_space_support_tt
				NOT = { check_variable = { FRA_peoples_support < 35 } }
			}
		}

		remove_Effect = {
			log = "[GetDateText]: [Root.GetName]: Decision FRA_prep_international_space_station"
			set_country_flag = FRA_preparing_launch
			add_timed_idea = {
				idea = FRA_idea_prep_the_launch
				days = 120
			}
			set_temp_variable = { temp_change = -5 }
			FRA_space_enthusiasm_effect = yes
		}
	}

	FRA_french_iss_launch = {
		cost = 100
		icon = GFX_decision_French_ISS_Launch

		days_remove = 30
		days_re_enable = 120

		available = {
			has_completed_focus = FRA_french_space
			has_country_flag = FRA_preparing_launch
			NOT = { has_idea = FRA_idea_prep_the_launch }
			NOT = {	has_idea = FRA_idea_space_research }
			custom_trigger_tooltip = {
				tooltip = fra_has_enough_space_support_tt
				NOT = { check_variable = { FRA_peoples_support < 35 } }
			}
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision FRA_french_iss_launch"
			add_timed_idea = {
				idea = FRA_idea_space_research
				days = 365
			}
			set_temp_variable = { temp_change = -5 }
			FRA_space_enthusiasm_effect = yes
			set_temp_variable = { temp_change = 0.350 }
			FRA_space_income_effect = yes
		}
	}
}

FRA_invest_LEB_decision_category = {
	FRA_LEB_invest_infrastructure_south = {
		cost = 100
		icon = political_actions

		days_remove = 140
		days_re_enable = 70
		fire_only_once = no

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision FRA_LEB_invest_infrastructure_south"
			LEB = {
				country_event = france_md.401
			}
		}

		ai_will_do = {
			factor = 1
			modifier = {
				factor = 0
				NOT = {
					has_global_flag = FRA_CHAOS_PATH
					has_government = communism
					has_government = nationalist
				}
			}
		}
	}


	FRA_LEB_invest_infrastructure_north = {
		cost = 100
		icon = political_actions

		days_remove = 140
		days_re_enable = 70
		fire_only_once = no

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision FRA_LEB_invest_infrastructure_north"
			LEB = {
				country_event = france_md.400
			}
		}

		ai_will_do = {
			factor = 1
			modifier = {
				factor = 0
				NOT = {
					has_global_flag = FRA_CHAOS_PATH
					has_government = communism
					has_government = nationalist
				}
			}
		}
	}

	FRA_LEB_invest_civilian_factory = {
		cost = 250
		icon = political_actions

		days_remove = 200
		days_re_enable = 70
		fire_only_once = no

		complete_effect = {
			log = "[GetDateText]: [ROOT.GetName]: Decision FRA_LEB_invest_civilian_factory"
			LEB = {
				country_event = france_md.402
			}
		}

		ai_will_do = {
			factor = 1
			modifier = {
				factor = 0
				NOT = {
					has_global_flag = FRA_CHAOS_PATH
					has_government = communism
					has_government = nationalist
				}
			}
		}
	}

	FRA_LEB_financial_help = {
		available = {
			LEB = { has_active_mission = bankruptcy_incoming_collapse }
			NOT = { has_active_mission = bankruptcy_incoming_collapse }
		}
		cost = 150
		icon = political_actions

		days_remove = 70
		days_re_enable = 140
		fire_only_once = no

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision FRA_LEB_financial_help"
			LEB = {
				country_event = france_md.403
			}
		}

		ai_will_do = {
			factor = 1
			modifier = {
				factor = 0
				NOT = {
					has_global_flag = FRA_CHAOS_PATH
					has_government = communism
					has_government = nationalist
				}
			}
		}
	}
}
FRA_LFI6th_decision_category = {
	FRA_6threp_mission = {
		available = { always = no }
		activation = { has_completed_focus = FRA_toward_6th_rep }
		days_mission_timeout = 365
		is_good = no
		fire_only_once = yes
		icon = GFX_france_mcdonalds_bombing
		cancel_trigger = {
			has_completed_focus = FRA_dawn_6th_rep
		}
		cancel_effect = {
			add_political_power = 100
			add_stability = 0.1
		}
		timeout_effect = {
			country_event = { id = france_md.236 }
		}
	}
	FRA_rally_support = {
		available = {
			has_active_mission = FRA_6threp_mission
		}
		cost = 150
		icon = political_actions

		days_remove = 50
		days_re_enable = 35
		fire_only_once = no

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision FRA_rally_support"
			set_temp_variable = { party_index = 18 }
			set_temp_variable = { party_popularity_increase = 0.1 }
			add_relative_party_popularity = yes
		}

		ai_will_do = {
			factor = 1
			modifier = {
				factor = 0
				NOT = {
					has_global_flag = FRA_CHAOS_PATH
					has_government = communism
					has_government = nationalist
				}
			}
		}
	}
	FRA_rally_ennemy = {
		available = {
			has_active_mission = FRA_6threp_mission
		}
		activation = { has_completed_focus = FRA_toward_6th_rep }
		days_mission_timeout = 100
		is_good = no
		fire_only_once = yes
		days_re_enable = 0
		icon = political_actions
		cancel_trigger = {
			OR = {
				has_completed_focus = FRA_dawn_6th_rep
				NOT = {
					has_active_mission = FRA_6threp_mission
				}
			}
		}
		timeout_effect = {
			country_event = { id = france_md.237 }
		}
	}
}

FRA_french_socialist_downfall_decision_category = {
	FRA_timebomb_mission = {
		available = { always = no }
		activation = { has_completed_focus = FRA_timebomb }
		days_mission_timeout = 365
		is_good = no
		fire_only_once = yes
		icon = GFX_france_mcdonalds_bombing
		cancel_trigger = {
			has_completed_focus = FRA_ps_win
		}
		cancel_effect = {
			remove_ideas = {
				FRA_resist_inevitable
				FRA_resist_inevitable2
				FRA_resist_inevitable3
				FRA_ineluctable_collapse
			}
		}
		timeout_effect = {
			add_political_power = -150
			country_event = { id = france_md.160 }
			remove_ideas = {
				FRA_resist_inevitable
				FRA_resist_inevitable2
				FRA_resist_inevitable3
			}
		}
	}
	FRA_support_decrease_mission = {
		available = { always = no }
		activation = { has_completed_focus = FRA_timebomb }
		days_mission_timeout = 100
		is_good = no
		fire_only_once = no
		days_re_enable = 0
		icon = political_actions
		cancel_trigger = {
			NOT = {
				has_idea = FRA_ineluctable_collapse
			}
		}
		timeout_effect = {
			country_event = { id = france_md.161 }
		}
	}
	FRA_campaign_against_extremism = {
		available = {
			has_active_mission = FRA_timebomb_mission
		}
		cost = 100
		icon = political_actions

		days_remove = 80
		days_re_enable = 0
		fire_only_once = no

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision FRA_campaign_against_extremism"
			set_temp_variable = { party_index = 3 }
			set_temp_variable = { party_popularity_increase = 0.1 }
			set_temp_variable = { temp_outlook_increase = 0.1 }
			add_relative_party_popularity = yes
			set_temp_variable = { party_index = 20 }
			set_temp_variable = { temp_outlook_increase = -0.05 }
			add_relative_party_popularity = yes
			set_temp_variable = { party_index = 18 }
			set_temp_variable = { temp_outlook_increase = -0.05 }
			add_relative_party_popularity = yes
		}

		ai_will_do = {
			factor = 1
			modifier = {
				factor = 0
				NOT = {
					has_global_flag = FRA_CHAOS_PATH
					has_government = communism
					has_government = nationalist
				}
			}
		}
	}
	FRA_campaign_clash_traitors = {
		available = {
			has_active_mission = FRA_timebomb_mission
		}
		cost = 200
		icon = political_actions

		days_remove = 80
		days_re_enable = 0
		fire_only_once = no

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision FRA_campaign_against_extremism"
			set_temp_variable = { party_index = 3 }
			set_temp_variable = { party_popularity_increase = 0.15 }
			add_relative_party_popularity = yes
			set_temp_variable = { party_index = 2 }
			set_temp_variable = { party_popularity_increase = -0.075 }
			set_temp_variable = { temp_outlook_increase = -0.075 }
			add_relative_party_popularity = yes
			set_temp_variable = { party_index = 18 }
			set_temp_variable = { party_popularity_increase = -0.075 }
			set_temp_variable = { temp_outlook_increase = -0.075 }
			add_relative_party_popularity = yes
		}

		ai_will_do = {
			factor = 1
			modifier = {
				factor = 0
				NOT = {
					has_global_flag = FRA_CHAOS_PATH
					has_government = communism
					has_government = nationalist
				}
			}
		}
	}
}
Reindustrilisation_category = {
	FRA_cons_reindustrialisation = {
		available = {always = no}
		activation = {
			OR = {
				has_completed_focus = FRA_wake_up
				has_completed_focus = FRA_social_populism
				has_completed_focus = FRA_recover
			}
		}
		days_mission_timeout = 365
		is_good = no
		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision timeout FRA_cons_reindustrialisation"
		}
		timeout_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision timeout FRA_cons_reindustrialisation"
		}
	}
}
FRA_frexit_decision_category = {
	FRA_boost_eurosceptism = {
		available = {
			has_completed_focus = FRA_neccesity_leaving
			has_idea = EU_member
		}
		cost = 100
		icon = political_actions

		days_remove = 70
		days_re_enable = 70
		fire_only_once = no

		remove_effect = {
			log = "[GetDateText]: [ROOT.GetName]: Decision FRA_boost_eurosceptism"

			set_temp_variable = { modify_eurosceptic = 0.1 }
			set_temp_variable = { modify_eurosceptic_target = FRA }
			eurosceptic_change = yes
			set_temp_variable = { party_index = 14 }
			set_temp_variable = { temp_outlook_increase = 0.075 }
			add_relative_party_popularity = yes
			set_temp_variable = { party_index = 20 }
			set_temp_variable = { temp_outlook_increase = 0.075 }
			add_relative_party_popularity = yes
			set_temp_variable = { treasury_change = -10 }
			modify_treasury_effect = yes
		}

		ai_will_do = {
			factor = 1
			modifier = {
				factor = 0
				NOT = {
					has_global_flag = FRA_CHAOS_PATH
					has_government = democratic
				}
			}
		}
	}
	FRA_disobey_treaties = {
		available = {
			has_completed_focus = FRA_anti_eu
			has_idea = EU_member
		}
		cost = 200
		icon = political_actions

		days_remove = 100
		days_re_enable = 70
		fire_only_once = no

		remove_effect = {
			log = "[GetDateText]: [ROOT.GetName]: Decision FRA_disobey_treaties"

			set_temp_variable = { modify_eurosceptic = 0.15 }
			set_temp_variable = { modify_eurosceptic_target = FRA }
			eurosceptic_change = yes
			set_temp_variable = { party_index = 14 }
			set_temp_variable = { temp_outlook_increase = 0.15 }
			add_relative_party_popularity = yes
			country_event = france_md.175
		}

		ai_will_do = {
			factor = 1
			modifier = {
				factor = 0
				NOT = {
					has_global_flag = FRA_CHAOS_PATH
					has_government = democratic
				}
			}
		}
	}
	FRA_denounce_eu_policies = {
		available = {
			has_completed_focus = FRA_anti_eu
			has_idea = EU_member
		}
		cost = 200
		icon = political_actions

		days_remove = 75
		days_re_enable = 35
		fire_only_once = no

		remove_effect = {
			log = "[GetDateText]: [ROOT.GetName]: Decision FRA_denounce_eu_policies"

			set_temp_variable = { modify_eurosceptic = 0.1 }
			set_temp_variable = { modify_eurosceptic_target = FRA }
			eurosceptic_change = yes
			set_temp_variable = { party_index = 18}
			set_temp_variable = { temp_outlook_increase = 0.1 }
			add_relative_party_popularity = yes
			set_temp_variable = { party_index = 20 }
			set_temp_variable = { temp_outlook_increase = 0.1 }
			add_relative_party_popularity = yes
		}

		ai_will_do = {
			factor = 1
			modifier = {
				factor = 0
				NOT = {
					has_global_flag = FRA_CHAOS_PATH
					has_government = democratic
				}
			}
		}
	}
	FRA_threaten_media = {
		available = {
			has_completed_focus = FRA_anti_eu
			has_idea = EU_member
		}
		cost = 200
		icon = political_actions

		days_remove = 60
		days_re_enable = 10
		fire_only_once = no

		remove_effect = {
			log = "[GetDateText]: [ROOT.GetName]: Decision FRA_boost_eurosceptism"

			set_temp_variable = { modify_eurosceptic = 0.125 }
			set_temp_variable = { modify_eurosceptic_target = FRA }
			eurosceptic_change = yes
			set_temp_variable = { party_index = 14 }
			set_temp_variable = { temp_outlook_increase = 0.15 }
			add_relative_party_popularity = yes
			add_stability = -0.1
			add_political_power = -75
		}
		modifier = {
			drift_defence_factor = 0.15
			stability_weekly_factor = -0.01
			political_power_gain = -0.25
		}

		ai_will_do = {
			factor = 1
			modifier = {
				factor = 0
				NOT = {
					has_global_flag = FRA_CHAOS_PATH
					has_government = democratic
				}
			}
		}
	}
}



FRA_french_africa_decision_category = {
	FRA_invest_in_NEPAD_education_decision = {
		visible = {
			has_completed_focus = FRA_NEPAD_development
			has_global_flag = AFRICA_nepad_focus
		}
		cost = 100
		icon = political_actions

		available = {
			custom_trigger_tooltip = {
				tooltip = FRA_nepad_program_counter_currently_tt
				has_global_flag = AFRICA_nepad_focus
			}
			NOT = {
				has_decision = FRA_invest_in_NEPAD_healthcare_decision
				has_decision = FRA_invest_in_NEPAD_social_care_decision
				has_decision = FRA_invest_in_NEPAD_economic_program_decision
			}
		}

		days_remove = 70
		fire_only_once = yes

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision FRA_invest_in_french_africa_education_decision"
			set_temp_variable = { temp_change = -0.05 }
			FRA_nepad_education_cost_modifier_effect = yes
			set_temp_variable = { temp_change = 0.15 }
			FRA_nepad_cost_increase_cost_effect = yes
			set_temp_variable = { treasury_change = gdp_total }
			multiply_temp_variable = { treasury_change = -0.03 }
			modify_treasury_effect = yes
		}

		ai_will_do = {
			factor = 1
			modifier = {
				factor = 0
				NOT = {
					has_global_flag = FRA_CHAOS_PATH
					has_government = communism
					has_government = nationalist
				}
			}
		}
	}

	FRA_invest_in_NEPAD_healthcare_decision = {
		visible = {
			has_completed_focus = FRA_NEPAD_development
			has_global_flag = AFRICA_nepad_focus
		}
		cost = 100
		icon = political_actions

		available = {
			custom_trigger_tooltip = {
				tooltip = FRA_nepad_program_counter_currently_tt
				has_global_flag = AFRICA_nepad_focus
			}
			NOT = {
				has_decision = FRA_invest_in_NEPAD_education_decision
				has_decision = FRA_invest_in_NEPAD_social_care_decision
				has_decision = FRA_invest_in_NEPAD_economic_program_decision
			}
		}

		days_remove = 70
		fire_only_once = yes

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision FRA_invest_in_NEPAD_healthcare_decision"
			set_temp_variable = { temp_change = -0.05 }
			FRA_nepad_health_cost_modifier_effect = yes
			set_temp_variable = { temp_change = 0.15 }
			FRA_nepad_cost_increase_cost_effect = yes
			set_temp_variable = { treasury_change = gdp_total }
			multiply_temp_variable = { treasury_change = -0.03 }
			modify_treasury_effect = yes
		}

		ai_will_do = {
			factor = 1
			modifier = {
				factor = 0
				NOT = {
					has_global_flag = FRA_CHAOS_PATH
					has_government = communism
					has_government = nationalist
				}
			}
		}
	}

	FRA_invest_in_NEPAD_social_care_decision = {
		visible = {
			has_completed_focus = FRA_NEPAD_development
			has_global_flag = AFRICA_nepad_focus
		}
		cost = 100
		icon = political_actions

		available = {
			custom_trigger_tooltip = {
				tooltip = FRA_nepad_program_counter_currently_tt
				has_global_flag = AFRICA_nepad_focus
			}
			NOT = {
				has_decision = FRA_invest_in_NEPAD_education_decision
				has_decision = FRA_invest_in_NEPAD_healthcare_decision
				has_decision = FRA_invest_in_NEPAD_economic_program_decision
			}
		}

		days_remove = 70
		fire_only_once = yes

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision FRA_invest_in_NEPAD_social_care_decision"
			set_temp_variable = { temp_change = -0.05 }
			FRA_nepad_social_cost_modifier_effect = yes
			set_temp_variable = { temp_change = 0.15 }
			FRA_nepad_cost_increase_cost_effect = yes
			set_temp_variable = { treasury_change = gdp_total }
			multiply_temp_variable = { treasury_change = -0.03 }
			modify_treasury_effect = yes
		}

		ai_will_do = {
			factor = 1
			modifier = {
				factor = 0
				NOT = {
					has_global_flag = FRA_CHAOS_PATH
					has_government = communism
					has_government = nationalist
				}
			}
		}
	}

	FRA_invest_in_NEPAD_economic_program_decision = {
		visible = {
			has_completed_focus = FRA_NEPAD_development
			has_global_flag = AFRICA_nepad_focus
		}
		cost = 100
		icon = political_actions

		available = {
			custom_trigger_tooltip = {
				tooltip = FRA_nepad_program_counter_currently_tt
				has_global_flag = AFRICA_nepad_focus
			}
			NOT = {
				has_decision = FRA_invest_in_NEPAD_education_decision
				has_decision = FRA_invest_in_NEPAD_healthcare_decision
				has_decision = FRA_invest_in_NEPAD_social_care_decision
			}
		}

		days_remove = 70

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision FRA_invest_in_NEPAD_economic_program_decision"
			set_temp_variable = { temp_change = 0.05 }
			FRA_nepad_buildings_construction_modifier_effect = yes
			set_temp_variable = { temp_change = 0.15 }
			FRA_nepad_cost_increase_cost_effect = yes
			set_temp_variable = { treasury_change = gdp_total }
			multiply_temp_variable = { treasury_change = -0.03 }
			modify_treasury_effect = yes
		}

		ai_will_do = {
			factor = 1
			modifier = {
				factor = 0
				NOT = {
					has_global_flag = FRA_CHAOS_PATH
					has_government = communism
					has_government = nationalist
				}
			}
		}
	}

	# TODO: Fix the Extension NEPAD Program
	# FRA_extend_the_NEPAD_program_decision = {
	# 	visible = {
	# 		has_completed_focus = FRA_NEPAD_development
	# 		has_global_flag = AFRICA_nepad_focus
	# 	}
	# 	cost = 100
	# 	icon = political_actions

	# 	available = {
	# 		custom_trigger_tooltip = {
	# 			tooltip = FRA_nepad_program_counter_currently_tt
	# 			has_global_flag = AFRICA_nepad_focus
	# 		}
	# 	}

	# 	days_remove = 70

	# 	complete_effect = {
	# 		log = "[GetDateText]: [Root.GetName]: Decision FRA_extend_the_NEPAD_program_decision"
	# 		set_temp_variable = { temp_change = 0.45 }
	# 		FRA_nepad_cost_increase_cost_effect = yes
	# 		set_temp_variable = { treasury_change = gdp_total }
	# 		multiply_temp_variable = { treasury_change = -0.03 }
	# 		modify_treasury_effect = yes
	# 		hidden_effect = {
	# 			FRA = {
	# 				modify_timed_idea = { idea = FRA_nepad_idea days = 365 }
	# 			}
	# 		}
	# 	}

	# 	ai_will_do = {
	# 		factor = 1
	# 		modifier = {
	# 			factor = 0
	# 			NOT = {
	# 				has_global_flag = FRA_CHAOS_PATH
	# 				has_government = communism
	# 				has_government = nationalist
	# 			}
	# 		}
	# 	}
	# }
}

# TODO: MISSION
FRA_french_construction_decision_category = {  # rename
    FRA_build_factories_mission = {  # rename
		icon = political_actions  # change
        allowed = { original_tag = FRA }
		available = { always = no }
		fire_only_once = yes  # change...?
        days_mission_timeout = 25  # testing -> 365
        activation = {  # modify focus or create individualized flags
			AND = {
				has_country_flag = FRA_timed_mission
				# has_completed_focus = FRA_reinvest_rural
				# lookup array_count in repo
				# check_variable = { FRA.states_for_timed_mission^0 = 0 }
				check_variable = { FRA.states_for_timed_mission^i > 1 }
			}
        }
        timeout_effect = {
			log = "[GetDateText]: [THIS.GetName]: focus FRA_build_factories_mission timeout"
			# custom_effect_tooltip = FRA_build_factories_mission_fail_tt

			if = {  # success
				limit = {
					check_variable = { FRA.factories_now > 4 }
				}
				newline = yes
				add_stability = 0.075
				set_party_index_to_ruling_party = yes
				set_temp_variable = { party_popularity_increase = 0.10 }
				add_relative_party_popularity = yes
				add_timed_idea = {
					idea = FRA_campaign_promises_kept_idea
					days = 200 # TODO: CHANGE?
				}
				# for reuse - move to function
				hidden_effect = {
					clear_variable = FRA.num_beginning_factories
					clear_variable = FRA.factories_now
					clear_array = FRA.states_for_timed_mission
				}
			}
			else = {  # failure
				newline = yes
				add_stability = -0.1
				set_party_index_to_ruling_party = yes
				set_temp_variable = { party_popularity_increase = -0.15 }
				add_relative_party_popularity = yes
				add_political_power = -100
				# for reuse - move to function
				hidden_effect = {
					clear_variable = FRA.num_beginning_factories
					clear_variable = FRA.factories_now
					clear_array = FRA.states_for_timed_mission
				}
			}
			clr_country_flag = FRA_timed_mission
        }
    }
}
FRA_french_communism_expansionism_decision_category = {
	FRA_red_benelux = {
		fire_only_once = yes
		days_remove = 35
		cost = 50
		visible = {
			OR = {
				AND = {
					country_exists = BEL
					NOT = {
						OR = {
							BEL = {
								is_in_faction_with = FRA
								is_subject_of = FRA
							}
						}
					}
				}
				AND = {
					country_exists = HOL
					NOT = {
						OR = {
							HOL = {
								is_in_faction_with = FRA
								is_subject_of = FRA
							}
						}
					}
				}
				AND = {
					country_exists = LUX
					NOT = {
						OR = {
							LUX = {
								is_in_faction_with = FRA
								is_subject_of = FRA
							}
						}
					}
				}
			}
		}
		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision FRA_red_benelux"
			set_temp_variable = { temp_change = 0.01 }
			FRA_WR_commie_drift = yes
			set_temp_variable = { temp_change = 0.1 }
			FRA_WR_coreattack =yes
			set_temp_variable = { temp_change = -0.2 }
			FRA_WR_education_cost =yes
			set_temp_variable = { temp_change = -0.025 }
			FRA_WR_stab =yes
			if = {
				limit = {
					NOT = {
						OR = {
							BEL = {
								is_in_faction_with = FRA
								is_subject_of = FRA
							}

						}
					}
				}
				BEL={
					country_event = france_md.313
				}
			}
			if = {
				limit = {
					NOT = {
						OR = {
							HOL = {
								is_in_faction_with = FRA
								is_subject_of = FRA
							}

						}
					}
				}
				HOL={
					country_event = france_md.313
				}
			}
			if = {
				limit = {
					NOT = {
						OR = {
							LUX = {
								is_in_faction_with = FRA
								is_subject_of = FRA
							}

						}
					}
				}
				LUX={
					country_event = france_md.313
				}
			}
		}
	}
	FRA_done_with_dumping = {
		fire_only_once = yes
		days_remove = 35
		cost = 50
		visible = {
			OR = {
				AND = {
					country_exists = SWI
					NOT = {
						OR = {
							SWI = {
								is_in_faction_with = FRA
								is_subject_of = FRA
							}
						}
					}
				}
				AND = {
					country_exists = ADO
					NOT = {
						OR = {
							ADO = {
								is_in_faction_with = FRA
								is_subject_of = FRA
							}
						}
					}
				}
				AND = {
					country_exists = MNC
					NOT = {
						OR = {
							MNC = {
								is_in_faction_with = FRA
								is_subject_of = FRA
							}
						}
					}
				}
			}
		}
		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision FRA_done_with_dumping"
			set_temp_variable = { temp_change = 0.01 }
			FRA_WR_commie_drift = yes
			set_temp_variable = { temp_change = -1 }
			FRA_WR_interest =yes
			set_temp_variable = { temp_change = 0.15 }
			FRA_WR_office_money = yes
			set_temp_variable = { temp_change = -0.025 }
			FRA_WR_stab =yes
			if = {
				limit = {
					NOT = {
						OR = {
							SWI = {
								is_in_faction_with = FRA
								is_subject_of = FRA
							}
						}
					}
				}
				SWI={
					country_event = france_md.313
				}
			}
			if = {
				limit = {
					NOT = {
						OR = {
							MNC = {
								is_in_faction_with = FRA
								is_subject_of = FRA
							}

						}
					}
				}
				MNC={
					country_event = france_md.313
				}
			}
			if = {
				limit = {
					NOT = {
						OR = {
							ADO = {
								is_in_faction_with = FRA
								is_subject_of = FRA
							}

						}
					}
				}
				ADO={
					country_event = france_md.313
				}
			}
		}
	}
	FRA_red_iberia = {
		fire_only_once = yes
		days_remove = 35
		cost = 25
		visible = {
			OR = {
				AND = {
					country_exists = SPR
					NOT = {
						OR = {
							SPR = {
								is_in_faction_with = FRA
								is_subject_of = FRA
							}
						}
					}
				}
				AND = {
					country_exists = POR
					NOT = {
						OR = {
							POR = {
								is_in_faction_with = FRA
								is_subject_of = FRA
							}
						}
					}
				}
			}
		}
		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision FRA_red_iberia"
			set_temp_variable = { temp_change = 0.02 }
			FRA_WR_commie_drift = yes
			set_temp_variable = { temp_change = -0.1 }
			FRA_WR_terrain_penalty =yes
			set_temp_variable = { temp_change = 0.15 }
			FRA_WR_construction = yes
			set_temp_variable = { temp_change = -0.05 }
			FRA_WR_stab =yes
			if = {
				limit = {
					NOT = {
						OR = {
							SPR = {
								is_in_faction_with = FRA
								is_subject_of = FRA
							}

						}
					}
				}
				create_wargoal = {
					type = puppet_wargoal_focus
					target = SPR
				}
			}
			if = {
				limit = {
					NOT = {
						OR = {
							POR = {
								is_in_faction_with = FRA
								is_subject_of = FRA
							}

						}
					}
				}
				POR={
					country_event = france_md.313
				}
			}
		}
	}
	FRA_ciao_bella = {
		fire_only_once = yes
		days_remove = 35
		cost = 25
		visible = {
			AND = {
				country_exists = ITA
				NOT = {
					OR = {
						ITA = {
							is_in_faction_with = FRA
							is_subject_of = FRA
						}
					}
				}
			}
		}
		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision FRA_ciao_bella"
			set_temp_variable = { temp_change = -0.1 }
			FRA_WR_propaganda = yes
			set_temp_variable = { temp_change = -0.15 }
			FRA_WR_terrain_penalty =yes
			set_temp_variable = { temp_change = 0.1 }
			FRA_WR_navy_speed = yes
			set_temp_variable = { temp_change = -0.05 }
			FRA_WR_stab =yes
			create_wargoal = {
				type = puppet_wargoal_focus
				target = ITA
			}
		}
	}
	FRA_avenge_long_knife = {
		fire_only_once = yes
		days_remove = 35
		cost = 25
		visible = {
			OR = {
				AND = {
					country_exists = GER
					NOT = {
						OR = {
							GER = {
								is_in_faction_with = FRA
								is_subject_of = FRA
							}
						}
					}
				}
				AND = {
					country_exists = AUS
					NOT = {
						OR = {
							AUS = {
								is_in_faction_with = FRA
								is_subject_of = FRA
							}
						}
					}
				}
			}
		}
		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision FRA_avenge_long_knife"
			set_temp_variable = { temp_change = 0.075 }
			FRA_WR_coredefence = yes
			set_temp_variable = { temp_change = 0.1 }
			FRA_WR_tank_attack =yes
			set_temp_variable = { temp_change = 0.15 }
			FRA_WR_tank_speed =yes
			set_temp_variable = { temp_change = 0.1 }
			FRA_WR_renable =yes
			set_temp_variable = { temp_change = -0.075 }
			FRA_WR_stab =yes
			if = {
				limit = {
					NOT = {
						OR = {
							GER = {
								is_in_faction_with = FRA
								is_subject_of = FRA
							}

						}
					}
				}
				create_wargoal = {
					type = puppet_wargoal_focus
					target = GER
				}
			}
			if = {
				limit = {
					NOT = {
						OR = {
							AUS = {
								is_in_faction_with = FRA
								is_subject_of = FRA
							}

						}
					}
				}
				AUS={
					country_event = france_md.313
				}
			}
		}
	}
	FRA_ostalgia = {
		fire_only_once = yes
		days_remove = 35
		cost = 25
		visible = {
			OR = {
				AND = {
					country_exists = CZE
					NOT = {
						OR = {
							CZE = {
								is_in_faction_with = FRA
								is_subject_of = FRA
							}
						}
					}
				}
				AND = {
					country_exists = SLO
					NOT = {
						OR = {
							SLO = {
								is_in_faction_with = FRA
								is_subject_of = FRA
							}
						}
					}
				}
				AND = {
					country_exists = POL
					NOT = {
						OR = {
							POL = {
								is_in_faction_with = FRA
								is_subject_of = FRA
							}
						}
					}
				}
				AND = {
					country_exists = HUN
					NOT = {
						OR = {
							HUN = {
								is_in_faction_with = FRA
								is_subject_of = FRA
							}
						}
					}
				}
				AND = {
					country_exists = BUL
					NOT = {
						OR = {
							BUL = {
								is_in_faction_with = FRA
								is_subject_of = FRA
							}
						}
					}
				}
				AND = {
					country_exists = LAT
					NOT = {
						OR = {
							LAT = {
								is_in_faction_with = FRA
								is_subject_of = FRA
							}
						}
					}
				}
				AND = {
					country_exists = LIT
					NOT = {
						OR = {
							LIT = {
								is_in_faction_with = FRA
								is_subject_of = FRA
							}
						}
					}
				}
				AND = {
					country_exists = EST
					NOT = {
						OR = {
							EST = {
								is_in_faction_with = FRA
								is_subject_of = FRA
							}
						}
					}
				}
				AND = {
					country_exists = ROM
					NOT = {
						OR = {
							ROM = {
								is_in_faction_with = FRA
								is_subject_of = FRA
							}
						}
					}
				}
				AND = {
					country_exists = MLV
					NOT = {
						OR = {
							MLV = {
								is_in_faction_with = FRA
								is_subject_of = FRA
							}
						}
					}
				}
			}
		}
		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision FRA_ostalgia"
			set_temp_variable = { temp_change = 0.05 }
			FRA_WR_coredefence = yes
			set_temp_variable = { temp_change = -0.15 }
			FRA_WR_consummer_goods = yes
			set_temp_variable = { temp_change = 0.1 }
			FRA_WR_agriculture_prod =yes
			set_temp_variable = { temp_change = -0.1 }
			FRA_WR_stab =yes
			if = {
				limit = {
					NOT = {
						OR = {
							CZE = {
								is_in_faction_with = FRA
								is_subject_of = FRA
							}

						}
					}
				}
				CZE={
					country_event = france_md.313
				}
			}
			if = {
				limit = {
					NOT = {
						OR = {
							SLO = {
								is_in_faction_with = FRA
								is_subject_of = FRA
							}

						}
					}
				}
				SLO={
					country_event = france_md.313
				}
			}
			if = {
				limit = {
					NOT = {
						OR = {
							POL = {
								is_in_faction_with = FRA
								is_subject_of = FRA
							}

						}
					}
				}
				POL={
					country_event = france_md.313
				}
			}
			if = {
				limit = {
					NOT = {
						OR = {
							HUN = {
								is_in_faction_with = FRA
								is_subject_of = FRA
							}

						}
					}
				}
				HUN={
					country_event = france_md.313
				}
			}
			if = {
				limit = {
					NOT = {
						OR = {
							BUL = {
								is_in_faction_with = FRA
								is_subject_of = FRA
							}

						}
					}
				}
				BUL={
					country_event = france_md.313
				}
			}
			if = {
				limit = {
					NOT = {
						OR = {
							LAT = {
								is_in_faction_with = FRA
								is_subject_of = FRA
							}

						}
					}
				}
				LAT={
					country_event = france_md.313
				}
			}
			if = {
				limit = {
					NOT = {
						OR = {
							LIT = {
								is_in_faction_with = FRA
								is_subject_of = FRA
							}

						}
					}
				}
				LIT={
					country_event = france_md.313
				}
			}
			if = {
				limit = {
					NOT = {
						OR = {
							EST = {
								is_in_faction_with = FRA
								is_subject_of = FRA
							}

						}
					}
				}
				EST={
					country_event = france_md.313
				}
			}
			if = {
				limit = {
					NOT = {
						OR = {
							ROM = {
								is_in_faction_with = FRA
								is_subject_of = FRA
							}

						}
					}
				}
				ROM={
					country_event = france_md.313
				}
			}
			if = {
				limit = {
					NOT = {
						OR = {
							MLV = {
								is_in_faction_with = FRA
								is_subject_of = FRA
							}

						}
					}
				}
				MLV={
					country_event = france_md.313
				}
			}
		}
	}
	FRA_birthplace_revolution = {
		fire_only_once = yes
		days_remove = 35
		cost = 25
		visible = {
			OR = {
				AND = {
					country_exists =SOV
					NOT = {
						OR = {
							SOV = {
								is_in_faction_with = FRA
								is_subject_of = FRA
							}
						}
					}
				}
				AND = {
					country_exists = BLR
					NOT = {
						OR = {
							BLR = {
								is_in_faction_with = FRA
								is_subject_of = FRA
							}
						}
					}
				}
				AND = {
					country_exists = UKR
					NOT = {
						OR = {
							UKR = {
								is_in_faction_with = FRA
								is_subject_of = FRA
							}
						}
					}
				}
			}
		}
		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision FRA_birthplace_revolution"
			set_temp_variable = { temp_change = 0.075 }
			FRA_WR_artillery_attack = yes
			set_temp_variable = { temp_change = 0.1 }
			FRA_WR_winter_climat = yes
			set_temp_variable = { temp_change = 0.15 }
			FRA_WR_agriculture_prod =yes
			set_temp_variable = { temp_change = -0.2 }
			FRA_WR_police_cost =yes
			set_temp_variable = { temp_change = -0.12 }
			FRA_WR_stab =yes
			if = {
				limit = {
					NOT = {
						OR = {
							SOV = {
								is_in_faction_with = FRA
								is_subject_of = FRA
							}

						}
					}
				}
				create_wargoal = {
					type = puppet_wargoal_focus
					target = SOV
				}
			}
			if = {
				limit = {
					NOT = {
						OR = {
							BLR = {
								is_in_faction_with = FRA
								is_subject_of = FRA
							}

						}
					}
				}
				BLR={
					country_event = france_md.313
				}
			}
			if = {
				limit = {
					NOT = {
						OR = {
							UKR = {
								is_in_faction_with = FRA
								is_subject_of = FRA
							}

						}
					}
				}
				UKR={
					country_event = france_md.313
				}
			}
		}
	}
	FRA_no_more_neutrality = {
		fire_only_once = yes
		days_remove = 35
		cost = 25
		visible = {
			OR = {
				AND = {
					country_exists =SWE
					NOT = {
						OR = {
							SWE = {
								is_in_faction_with = FRA
								is_subject_of = FRA
							}
						}
					}
				}
				AND = {
					country_exists = DEN
					NOT = {
						OR = {
							DEN = {
								is_in_faction_with = FRA
								is_subject_of = FRA
							}
						}
					}
				}
				AND = {
					country_exists = NOR
					NOT = {
						OR = {
							NOR = {
								is_in_faction_with = FRA
								is_subject_of = FRA
							}
						}
					}
				}
				AND = {
					country_exists = FIN
					NOT = {
						OR = {
							FIN = {
								is_in_faction_with = FRA
								is_subject_of = FRA
							}
						}
					}
				}
			}
		}
		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision FRA_no_more_neutrality"
			set_temp_variable = { temp_change = 0.075 }
			FRA_WR_social_cost = yes
			set_temp_variable = { temp_change = 0.15 }
			FRA_WR_winter_climat = yes
			set_temp_variable = { temp_change = 0.15 }
			FRA_WR_prod_effi =yes
			set_temp_variable = { temp_change = 0.05 }
			FRA_WR_reco =yes
			set_temp_variable = { temp_change = -0.075 }
			FRA_WR_stab =yes
			if = {
				limit = {
					NOT = {
						OR = {
							SWE = {
								is_in_faction_with = FRA
								is_subject_of = FRA
							}

						}
					}
				}
				SWE={
					country_event = france_md.313
				}
			}
			if = {
				limit = {
					NOT = {
						OR = {
							DEN = {
								is_in_faction_with = FRA
								is_subject_of = FRA
							}

						}
					}
				}
				DEN={
					country_event = france_md.313
				}
			}
			if = {
				limit = {
					NOT = {
						OR = {
							NOR = {
								is_in_faction_with = FRA
								is_subject_of = FRA
							}

						}
					}
				}
				NOR={
					country_event = france_md.313
				}
			}
			if = {
				limit = {
					NOT = {
						OR = {
							FIN = {
								is_in_faction_with = FRA
								is_subject_of = FRA
							}

						}
					}
				}
				FIN={
					country_event = france_md.313
				}
			}
		}
	}
	FRA_cold_war2 = {
		fire_only_once = yes
		days_remove = 35
		cost = 25
		visible = {
			OR = {
				AND = {
					country_exists = USA
					NOT = {
						OR = {
							USA = {
								is_in_faction_with = FRA
								is_subject_of = FRA
							}
						}
					}
				}
				AND = {
					country_exists = ENG
					NOT = {
						OR = {
							ENG = {
								is_in_faction_with = FRA
								is_subject_of = FRA
							}
						}
					}
				}
			}
		}
		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision FRA_cold_war2"
			set_temp_variable = { temp_change = 0.1 }
			FRA_WR_amphibious_invasion_speed = yes
			set_temp_variable = { temp_change = 0.15 }
			FRA_WR_navy_range = yes
			set_temp_variable = { temp_change = 0.05 }
			FRA_WR_navy_coord =yes
			set_temp_variable = { temp_change = 0.075 }
			FRA_WR_special_forces_attack =yes
			set_temp_variable = { temp_change = -0.15 }
			FRA_WR_stab =yes
			if = {
				limit = {
					NOT = {
						OR = {
							USA = {
								is_in_faction_with = FRA
								is_subject_of = FRA
							}

						}
					}
				}
				create_wargoal = {
					type = puppet_wargoal_focus
					target = USA
				}
			}
			if = {
				limit = {
					NOT = {
						OR = {
							ENG = {
								is_in_faction_with = FRA
								is_subject_of = FRA
							}

						}
					}
				}
				create_wargoal = {
					type = puppet_wargoal_focus
					target = ENG
				}
			}
		}
	}
	FRA_red_Europe = {
		fire_only_once = yes
		days_remove = 50
		cost = 100
		visible = {
			AND = {
				is_subject = no
				any_country = {
					limit = {
						AND = {
							is_in_europe = yes
							NOT = {
								is_in_faction_with = FRA
								is_subject_of = FRA
							}
						}
					}
				}
			}
		}
		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision FRA_red_Europe"
			country_event = france_md.316
			every_other_country = {
				limit = {
					AND = {
						is_in_europe = yes
						NOT = {
							is_in_faction_with = FRA
							is_subject_of = FRA
						}
					}
				}
				FRA = {
					create_wargoal = {
						type = puppet_wargoal_focus
						target = FROM
					}
				}
			}
		}
	}
}
FRA_3rd_empire_decision_category = {#TODO add claim for all
	FRA_reunionism = {#claim belgium holland luxembourg
		fire_only_once = yes
		days_remove = 35
		cost = 50
		visible = {
			OR = {
				country_exists = BEL
				country_exists = HOL
				country_exists = LUX
			}
		}
		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision FRA_reunionism"
			country_event = france_md.317
			if = {
				limit = {country_exists = BEL}
				create_wargoal = {
						type = annex_everything
						target = BEL
				}
			}
			if = {
				limit = {country_exists = HOL}
				create_wargoal = {
						type = annex_everything
						target = HOL
				}
			}
			if = {
				limit = {country_exists = LUX}
				create_wargoal = {
						type = annex_everything
						target = LUX
				}
			}
			965 = {add_claim_by = FRA}
			52 = {add_claim_by = FRA}
			51 = {add_claim_by = FRA}
			50 = {add_claim_by = FRA}
			53 = {add_claim_by = FRA}
			47 = {add_claim_by = FRA}
			46 = {add_claim_by = FRA}
			48 = {add_claim_by = FRA}
			1169 = {add_claim_by = FRA}
		}
	}
	FRA_Never_another_guerilla = {#claim catalogna
		fire_only_once = yes
		days_remove = 35
		cost = 50
		visible = {
			OR = {
				country_exists = SPR
				country_exists = POR
			}
		}
		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision FRA_Never_another_guerilla"
			country_event = france_md.318
			if = {
				limit = {country_exists = SPR}
				create_wargoal = {
						type = puppet_wargoal_focus
						target = SPR
				}
			}
			if = {
				limit = {country_exists = POR}
				create_wargoal = {
						type = puppet_wargoal_focus
						target = POR
				}
			}
		}
	}
	FRA_Avenge_Sedan = {#claim Rheinland
		fire_only_once = yes
		days_remove = 35
		cost = 50
		visible = {
			country_exists = GER
		}
		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision FRA_Avenge_Sedan"
			country_event = france_md.319
			if = {
				limit = {country_exists = GER}
				create_wargoal = {
						type = puppet_wargoal_focus
						target = GER
				}
			}
		}
	}
	FRA_crush_coalition = {#claim north of italy
		fire_only_once = yes
		days_remove = 35
		cost = 50
		visible = {
			OR = {
				country_exists = AUS
				country_exists = HUN
				country_exists = ITA
				country_exists = SLO
				country_exists = CZE
			}

		}
		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision FRA_crush_coalition"
			country_event = france_md.320
			if = {
				limit = {country_exists = AUS}
				create_wargoal = {
						type = puppet_wargoal_focus
						target = AUS
				}
			}
			if = {
				limit = {country_exists = HUN}
				create_wargoal = {
						type = puppet_wargoal_focus
						target = HUN
				}
			}
			if = {
				limit = {country_exists = ITA}
				create_wargoal = {
						type = puppet_wargoal_focus
						target = ITA
				}
			}
			if = {
				limit = {country_exists = CZE}
				create_wargoal = {
						type = puppet_wargoal_focus
						target = CZE
				}
			}
			if = {
				limit = {country_exists = SLO}
				create_wargoal = {
						type = puppet_wargoal_focus
						target = SLO
				}
			}
		}
	}
	FRA_prepare_russia = {
		fire_only_once = yes
		days_remove = 35
		cost = 50
		visible = {
			OR = {
				country_exists = POL
				country_exists = ROM
				country_exists = LAT
				country_exists = EST
				country_exists = LIT
			}

		}
		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision FRA_prepare_russia"
			country_event = france_md.322
			if = {
				limit = {
					AND = {
						country_exists = POL
						NOT = {POL = {is_in_faction_with = FRA}}
					}
				}
				create_wargoal = {
					type = puppet_wargoal_focus
					target = POL
				}
			}
			else_if = {
				limit = {
					AND = {
						country_exists = POL
						POL = {is_in_faction_with = FRA}
					}
				}
				POL = {
					country_event = france_md.321
				}
			}
			if = {
				limit = {
					AND = {
						country_exists = ROM
						NOT = {ROM = {is_in_faction_with = FRA}}
					}
				}
				create_wargoal = {
					type = puppet_wargoal_focus
					target = ROM
				}
			}
			else_if = {
				limit = {
					AND = {
						country_exists = ROM
						ROM = {is_in_faction_with = FRA}
					}
				}
				ROM = {
					country_event = france_md.321
				}
			}
			if = {
				limit = {
					AND = {
						country_exists = LAT
						NOT = {LAT = {is_in_faction_with = FRA}}
					}
				}
				create_wargoal = {
					type = puppet_wargoal_focus
					target = LAT
				}
			}
			else_if = {
				limit = {
					AND = {
						country_exists = LAT
						LAT = {is_in_faction_with = FRA}
					}
				}
				LAT = {
					country_event = france_md.321
				}
			}
			if = {
				limit = {
					AND = {
						country_exists = EST
						NOT = {EST = {is_in_faction_with = FRA}}
					}
				}
				create_wargoal = {
					type = puppet_wargoal_focus
					target = EST
				}
			}
			else_if = {
				limit = {
					AND = {
						country_exists = EST
						EST = {is_in_faction_with = FRA}
					}
				}
				EST = {
					country_event = france_md.321
				}
			}
			if = {
				limit = {
					AND = {
						country_exists = LIT
						NOT = {LIT = {is_in_faction_with = FRA}}
					}
				}
				create_wargoal = {
					type = puppet_wargoal_focus
					target = LIT
				}
			}
			else_if = {
				limit = {
					AND = {
						country_exists = LIT
						LIT = {is_in_faction_with = FRA}
					}
				}
				LIT = {
					country_event = france_md.321
				}
			}
		}
	}
	FRA_no_more_berezina = {
		fire_only_once = yes
		days_remove = 35
		cost = 50
		visible = {
			OR = {
				country_exists = SOV
				country_exists = UKR
				country_exists = BLR
			}

		}
		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision FRA_no_more_berezina"
			country_event = france_md.475
			if = {
				limit = {country_exists = SOV}
				create_wargoal = {
						type = puppet_wargoal_focus
						target = SOV
				}
			}
			if = {
				limit = {country_exists = UKR}
				create_wargoal = {
						type = puppet_wargoal_focus
						target = UKR
				}
			}
			if = {
				limit = {country_exists = BLR}
				create_wargoal = {
						type = puppet_wargoal_focus
						target = BLR
				}
			}
		}
	}
	FRA_the_Foe = {
		fire_only_once = yes
		days_remove = 35
		cost = 50
		visible = {
			country_exists = ENG
		}
		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision FRA_the_Foe"
			country_event = france_md.476
			if = {
				limit = {country_exists = ENG}
				create_wargoal = {
						type = puppet_wargoal_focus
						target = ENG
				}
			}
		}
	}
	FRA_new_order = {
		fire_only_once = yes
		days_remove = 35
		cost = 50
		visible = {
			OR = {
				SOV = { is_subject_of = FRA }
				GER = { is_subject_of = FRA }
				ENG = { is_subject_of = FRA }
			}
		}
		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision FRA_new_order"
			country_event = france_md.477
			every_other_country = {
				limit = { is_subject_of = FRA }
				add_ideas = FRA_exploitation
			}
		}
	}
}
FRA_royalist_expansion_decision_category = {#TODO add claim for all
	FRA_Expansion_policy = {
		fire_only_once = yes
		days_remove = 35
		cost = 50
		visible = {
			NOT = {
				OR = {
					has_idea = FRA_expansion_crusades
					has_idea = FRA_expansion_europe
				}
			}
		}
		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision FRA_European_crusade"
			country_event = france_md.478
		}
	}
	FRA_frankish_nest = {#claim belgium holland luxembourg
		fire_only_once = yes
		days_remove = 35
		cost = 50
		visible = {
			AND = {
				FRA_expansion_europe
				OR = {
					country_exists = BEL
					country_exists = HOL
					country_exists = LUX
				}
			}
		}
		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision FRA_frankish_nest"
			if = {
				limit = {country_exists = BEL}
				BEL = {
					country_event = france_md.479
				}
			}
			if = {
				limit = {country_exists = HOL}
				HOL = {
					country_event = france_md.479
				}
			}
			if = {
				limit = {country_exists = LUX}
				LUX = {
					country_event = france_md.479
				}
			}
			965 = {add_claim_by = FRA}
			52 = {add_claim_by = FRA}
			51 = {add_claim_by = FRA}
			50 = {add_claim_by = FRA}
			53 = {add_claim_by = FRA}
			47 = {add_claim_by = FRA}
			46 = {add_claim_by = FRA}
			48 = {add_claim_by = FRA}
			1169 = {add_claim_by = FRA}
		}
	}
	FRA_heir_of_charles_le_chauve = {#claim germany
		fire_only_once = yes
		days_remove = 35
		cost = 50
		visible = {
			AND = {
				FRA_expansion_europe
				country_exists = GER

			}
		}
		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision FRA_heir_of_charles_le_chauve"
			if = {
				limit = {country_exists = GER}
				GER = {
					country_event = france_md.479
				}
			}
			967 = {add_claim_by = FRA}
			40 = {add_claim_by = FRA}
			42 = {add_claim_by = FRA}
			41 = {add_claim_by = FRA}
			43 = {add_claim_by = FRA}
			964 = {add_claim_by = FRA}
			966 = {add_claim_by = FRA}
			39 = {add_claim_by = FRA}
			38 = {add_claim_by = FRA}
			1080 = {add_claim_by = FRA}
			37 = {add_claim_by = FRA}
			539 = {add_claim_by = FRA}
			45 = {add_claim_by = FRA}
			1079 = {add_claim_by = FRA}
			44 = {add_claim_by = FRA}
			545 = {add_claim_by = FRA}
			972 = {add_claim_by = FRA}
		}
	}
	FRA_protect_papacy = {#claim northern italy switzerland and austria
		fire_only_once = yes
		days_remove = 35
		cost = 50
		visible = {
			AND = {
				FRA_expansion_europe
				OR = {
					country_exists = ITA
					country_exists = SWI
					country_exists = AUS
				}
			}
		}
		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision FRA_protect_papacy"
			if = {
				limit = {country_exists = SWI}
				SWI = {
					country_event = france_md.479
				}
			}
			if = {
				limit = {country_exists = AUS}
				AUS = {
					country_event = france_md.479
				}
			}
			if = {
				limit = {country_exists = ITA}
				ITA = {
					country_event = france_md.482
				}
			}
			72 = {add_claim_by = FRA}
			74 = {add_claim_by = FRA}
			73 = {add_claim_by = FRA}
			76 = {add_claim_by = FRA}
			75 = {add_claim_by = FRA}
			286 = {add_claim_by = FRA}
			1060 = {add_claim_by = FRA}
			1059 = {add_claim_by = FRA}
			77 = {add_claim_by = FRA}
			80 = {add_claim_by = FRA}
			78 = {add_claim_by = FRA}
			285 = {add_claim_by = FRA}
			265 = {add_claim_by = FRA}
			1061 = {add_claim_by = FRA}
			79 = {add_claim_by = FRA}
		}
	}
}

# Joint Weapon Projects Category
FRA_joint_weapon_projects_category = {

	# FCAS/MGCS - Joint Franco-German weapon project (Socialists)
	FRA_joint_project_germany = {
		icon = GFX_decision_generic_air_naval_exercise

		cost = 150
		days_remove = 180
		fire_only_once = yes

		visible = {
			country_exists = GER
			NOT = { has_war_with = GER }
			OR = {
				western_social_democrats_are_in_power = yes
				emerging_communist_state_are_in_power = yes
			}
		}

		available = {
			GER = { is_subject = no }
			NOT = { has_war_with = GER }
			custom_trigger_tooltip = {
				tooltip = FRA_socialist_government_required_tt
				OR = {
					western_social_democrats_are_in_power = yes
					emerging_communist_state_are_in_power = yes
				}
			}
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision FRA_joint_project_germany"
			set_country_flag = FRA_proposed_joint_project_germany
			GER = { country_event = { id = france_md.335 days = 5 } }
		}

		remove_effect = {
			if = {
				limit = { has_country_flag = FRA_germany_accepted_joint_project }
				add_tech_bonus = {
					name = FRA_joint_project_germany
					bonus = 0.25
					uses = 2
					category = CAT_mbt
				}
				add_tech_bonus = {
					name = FRA_joint_project_germany
					bonus = 0.25
					uses = 2
					category = CAT_jet_fighter
				}
				add_ideas = FRA_FCAS_MGCS_project
				clr_country_flag = FRA_germany_accepted_joint_project
			}
			clr_country_flag = FRA_proposed_joint_project_germany
		}

		ai_will_do = {
			factor = 10
			modifier = {
				factor = 2
				has_opinion = { target = GER value > 50 }
			}
		}
	}

	# Joint Franco-Russian weapon project (National Rally/Nationalists)
	FRA_joint_project_russia = {
		icon = GFX_decision_generic_air_naval_exercise

		cost = 150
		days_remove = 180
		fire_only_once = yes

		visible = {
			country_exists = RUS
			NOT = { has_war_with = RUS }
			OR = {
				nationalist_right_wing_populists_are_in_power = yes
				nationalist_monarchists_are_in_power = yes
			}
		}

		available = {
			RUS = { is_subject = no }
			NOT = { has_war_with = RUS }
			NOT = { has_idea = EU_member }
			custom_trigger_tooltip = {
				tooltip = FRA_nationalist_government_required_tt
				OR = {
					nationalist_right_wing_populists_are_in_power = yes
					nationalist_monarchists_are_in_power = yes
				}
			}
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision FRA_joint_project_russia"
			set_country_flag = FRA_proposed_joint_project_russia
			RUS = { country_event = { id = france_md.337 days = 5 } }
		}

		remove_effect = {
			if = {
				limit = { has_country_flag = FRA_russia_accepted_joint_project }
				add_tech_bonus = {
					name = FRA_joint_project_russia
					bonus = 0.25
					uses = 2
					category = CAT_ifv
				}
				add_tech_bonus = {
					name = FRA_joint_project_russia
					bonus = 0.25
					uses = 2
					category = CAT_apc
				}
				add_ideas = FRA_RUS_joint_project
				clr_country_flag = FRA_russia_accepted_joint_project
			}
			clr_country_flag = FRA_proposed_joint_project_russia
		}

		ai_will_do = {
			factor = 5
			modifier = {
				factor = 2
				has_opinion = { target = RUS value > 50 }
			}
			modifier = {
				factor = 0
				is_historical_focus_on = yes
			}
		}
	}
}

#################################################
# Extremist Foreign Policy Decisions
# Author: Claude Code
#################################################

FRA_extremist_foreign_policy_category = {
	icon = generic_nationalism

	allowed = { original_tag = FRA }

	visible = {
		OR = {
			FRA_on_extremist_path = yes
			FRA_on_gaullist_path = yes
		}
	}

	# Rejoin NATO Integrated Command (reversal decision for Gaullist path)
	FRA_rejoin_nato_command = {
		icon = GFX_decision_generic_military_parade

		cost = 100
		days_remove = 90
		fire_only_once = yes

		visible = {
			has_idea = FRA_distance_nato
			NOT = { has_idea = FRA_left_nato }
		}

		available = {
			has_war = no
			NOT = { has_idea = FRA_anti_american_stance }
			USA = { is_subject = no }
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision FRA_rejoin_nato_command"
			add_political_power = -75
		}

		remove_effect = {
			remove_ideas = FRA_distance_nato
			remove_ideas = FRA_strategic_independance
			USA = {
				add_opinion_modifier = {
					target = FRA
					modifier = small_increase
				}
			}
			every_country = {
				limit = {
					has_idea = NATO_member
					NOT = { tag = FRA }
				}
				add_opinion_modifier = {
					target = FRA
					modifier = small_increase
				}
			}
		}

		ai_will_do = {
			factor = 0
			modifier = {
				factor = 5
				date > 2022.2.24
				SOV = { has_war_with = UKR }
			}
		}
	}

	# Seek Russian Partnership (for Gaullist/FN paths)
	FRA_seek_russian_partnership = {
		icon = GFX_decision_generic_operation

		cost = 75
		days_remove = 60
		fire_only_once = yes

		visible = {
			OR = {
				FRA_on_gaullist_path = yes
				FRA_on_far_right_path = yes
			}
			NOT = { has_country_flag = FRA_sought_russian_partnership }
			date < 2022.2.24
		}

		available = {
			NOT = { has_war_with = SOV }
			SOV = { is_subject = no }
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision FRA_seek_russian_partnership"
			set_country_flag = FRA_sought_russian_partnership
			SOV = {
				country_event = { id = france_md.802 days = 5 }
			}
		}

		remove_effect = {
			add_opinion_modifier = {
				target = SOV
				modifier = improved_relations
			}
			USA = {
				add_opinion_modifier = {
					target = FRA
					modifier = small_decrease
				}
			}
		}

		ai_will_do = {
			factor = 3
			modifier = {
				factor = 0
				is_historical_focus_on = yes
			}
		}
	}

	# Recognize Palestine (for far-left paths)
	FRA_recognize_palestine = {
		icon = GFX_decision_generic_agreement

		cost = 50
		fire_only_once = yes

		visible = {
			FRA_on_far_left_path = yes
			NOT = { has_country_flag = FRA_supports_two_state_solution }
		}

		available = {
			has_political_power > 50
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision FRA_recognize_palestine"
			set_country_flag = FRA_supports_two_state_solution
			add_stability = 0.02
			ISR = {
				add_opinion_modifier = {
					target = FRA
					modifier = diplomatic_rejection
				}
			}
			every_country = {
				limit = {
					OR = {
						tag = EGY
						tag = JOR
						tag = SAU
						tag = UAE
					}
				}
				add_opinion_modifier = {
					target = FRA
					modifier = small_increase
				}
			}
		}

		ai_will_do = {
			factor = 5
		}
	}

	# Condemn Russian Aggression (reversal for Ukraine War)
	FRA_condemn_russia_ukraine = {
		icon = GFX_decision_generic_military_parade

		cost = 100
		fire_only_once = yes

		visible = {
			date > 2022.2.24
			SOV = { has_war_with = UKR }
			OR = {
				has_idea = FRA_fn_russia_alignment_idea
				has_idea = FRA_gaullist_foreign_policy
			}
		}

		available = {
			has_political_power > 100
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision FRA_condemn_russia_ukraine"
			add_political_power = -100
			add_stability = -0.05
			if = {
				limit = { has_idea = FRA_fn_russia_alignment_idea }
				remove_ideas = FRA_fn_russia_alignment_idea
			}
			SOV = {
				add_opinion_modifier = {
					target = FRA
					modifier = major_breach_of_trust
				}
			}
			USA = {
				add_opinion_modifier = {
					target = FRA
					modifier = small_increase
				}
			}
			every_country = {
				limit = { has_idea = NATO_member }
				add_opinion_modifier = {
					target = FRA
					modifier = small_increase
				}
			}
		}

		ai_will_do = {
			factor = 10
		}
	}

	# Pursue Non-Aligned Leadership (for LFI)
	FRA_non_aligned_initiative = {
		icon = GFX_decision_generic_nationalism

		cost = 100
		days_remove = 180
		fire_only_once = yes

		visible = {
			has_idea = FRA_lfi_non_aligned_policy
			NOT = { has_country_flag = FRA_leading_non_aligned }
		}

		available = {
			has_political_power > 100
			NOT = { has_idea = NATO_member }
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision FRA_non_aligned_initiative"
			set_country_flag = FRA_leading_non_aligned
		}

		remove_effect = {
			add_political_power = 100
			add_stability = 0.03
			IND = {
				add_opinion_modifier = {
					target = FRA
					modifier = improved_relations
				}
			}
			BRA = {
				add_opinion_modifier = {
					target = FRA
					modifier = improved_relations
				}
			}
		}

		ai_will_do = {
			factor = 5
		}
	}

	# Strengthen Socialist Solidarity (for PCF)
	FRA_socialist_solidarity = {
		icon = GFX_decision_generic_propaganda

		cost = 75
		days_remove = 90
		fire_only_once = yes

		visible = {
			has_idea = FRA_communist_foreign_policy
			NOT = { has_country_flag = FRA_socialist_solidarity_active }
		}

		available = {
			has_political_power > 75
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision FRA_socialist_solidarity"
			set_country_flag = FRA_socialist_solidarity_active
		}

		remove_effect = {
			add_political_power = 50
			CUB = {
				add_opinion_modifier = {
					target = FRA
					modifier = improved_relations
				}
			}
			VEN = {
				add_opinion_modifier = {
					target = FRA
					modifier = improved_relations
				}
			}
			# Boost communist popularity
			set_temp_variable = { party_index = 18 }
			set_temp_variable = { party_popularity_increase = 0.02 }
			add_relative_party_popularity = yes
		}

		ai_will_do = {
			factor = 5
		}
	}

	# Block EU Federalization (for sovereigntist paths)
	FRA_veto_eu_federalization = {
		icon = GFX_decision_generic_nationalism

		cost = 50
		fire_only_once = no
		days_re_enable = 365

		visible = {
			has_idea = FRA_sovereigntist_eu_policy
			has_idea = EU_member
		}

		available = {
			has_political_power > 50
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision FRA_veto_eu_federalization"
			add_political_power = 25
			add_stability = 0.01
			every_country = {
				limit = {
					has_idea = EU_member
					NOT = { tag = FRA }
				}
				add_opinion_modifier = {
					target = FRA
					modifier = FRA_eu_obstruction
				}
			}
		}

		ai_will_do = {
			factor = 3
		}
	}
}

# Environmental Policy Decisions
FRA_environmental_policy_decisions = {
	# Decision to reverse the extraction ban
	FRA_reverse_extraction_ban = {
		icon = generic_industry

		available = {
			has_idea = FRA_green_extraction_ban
			NOT = { neutrality_neutral_green_in_power_or_coalition = yes }
		}

		visible = {
			has_idea = FRA_green_extraction_ban
		}

		cost = 100
		days_remove = 180

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision FRA_reverse_extraction_ban"
			remove_ideas = FRA_green_extraction_ban
			add_stability = -0.05
			# Greens lose support
			set_temp_variable = { party_index = 17 }
			set_temp_variable = { party_popularity_increase = -0.10 }
			set_temp_variable = { temp_outlook_increase = -0.10 }
			add_relative_party_popularity = yes
			# Economic boost from reopening extraction
			add_political_power = 50
		}

		ai_will_do = {
			factor = 0
			modifier = {
				add = 5
				has_war = yes
			}
		}
	}
}

# Counter-Terrorism Decisions
# Author: Claude Code
FRA_counter_terrorism_category = {

	# Activate Vigipirate security plan
	FRA_activate_vigipirate = {
		icon = GFX_decision_generic_military_parade

		allowed = { original_tag = FRA }

		available = {
			NOT = { has_idea = FRA_heightened_security }
			NOT = { has_idea = FRA_state_of_emergency_terrorism }
		}

		visible = {
			OR = {
				FRA_vulnerable_to_terrorism = yes
				has_country_flag = FRA_terror_level_1
				has_country_flag = FRA_terror_level_2
				has_country_flag = FRA_terror_level_3
				has_idea = FRA_terror_threat_elevated
				has_idea = FRA_terror_threat_critical
			}
		}

		cost = 50
		days_remove = 180

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision FRA_activate_vigipirate"
			add_ideas = FRA_heightened_security
			custom_effect_tooltip = FRA_vigipirate_activated_tt
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove FRA_activate_vigipirate"
			if = {
				limit = {
					NOT = { has_idea = FRA_state_of_emergency_terrorism }
				}
				remove_ideas = FRA_heightened_security
			}
		}

		ai_will_do = {
			factor = 10
			modifier = {
				add = 20
				has_country_flag = FRA_terror_level_2
			}
			modifier = {
				add = 30
				has_country_flag = FRA_terror_level_3
			}
			modifier = {
				add = 50
				has_idea = FRA_terror_threat_critical
			}
		}
	}

	# Declare state of emergency (stronger protection)
	FRA_declare_state_of_emergency = {
		icon = GFX_decision_generic_civil_war

		allowed = { original_tag = FRA }

		available = {
			OR = {
				has_idea = FRA_terror_threat_critical
				has_country_flag = FRA_terror_level_3
				has_country_flag = FRA_major_terror_attack
			}
		}

		visible = {
			OR = {
				has_country_flag = FRA_terror_level_2
				has_country_flag = FRA_terror_level_3
				has_idea = FRA_terror_threat_critical
				has_idea = FRA_terror_threat_elevated
			}
		}

		cost = 100
		fire_only_once = no

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision FRA_declare_state_of_emergency"
			if = {
				limit = { has_idea = FRA_heightened_security }
				remove_ideas = FRA_heightened_security
			}
			add_ideas = FRA_state_of_emergency_terrorism
			add_stability = -0.02
			custom_effect_tooltip = FRA_state_of_emergency_declared_tt
		}

		ai_will_do = {
			factor = 20
			modifier = {
				add = 50
				has_country_flag = FRA_terror_level_3
			}
			modifier = {
				add = 100
				has_country_flag = FRA_major_terror_attack
			}
		}
	}

	# End state of emergency
	FRA_end_state_of_emergency = {
		icon = GFX_decision_generic_political_discourse

		allowed = { original_tag = FRA }

		available = {
			has_idea = FRA_state_of_emergency_terrorism
			NOT = { has_country_flag = FRA_terror_level_3 }
			has_stability > 0.35
		}

		visible = {
			has_idea = FRA_state_of_emergency_terrorism
		}

		cost = 25

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision FRA_end_state_of_emergency"
			remove_ideas = FRA_state_of_emergency_terrorism
			add_stability = 0.03
			add_political_power = 50
			custom_effect_tooltip = FRA_state_of_emergency_ended_tt
		}

		ai_will_do = {
			factor = 5
			modifier = {
				add = 20
				has_stability > 0.50
				NOT = { has_country_flag = FRA_terror_level_2 }
			}
		}
	}

	# Increase intelligence funding
	FRA_intelligence_surge = {
		icon = GFX_decision_generic_intelligence_operation

		allowed = { original_tag = FRA }

		available = {
			NOT = { has_country_flag = FRA_intelligence_surge }
		}

		visible = {
			OR = {
				FRA_vulnerable_to_terrorism = yes
				has_country_flag = FRA_terror_level_1
				has_country_flag = FRA_terror_level_2
				has_country_flag = FRA_terror_level_3
			}
		}

		cost = 75
		days_remove = 365

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision FRA_intelligence_surge"
			set_country_flag = FRA_intelligence_surge
			set_temp_variable = { treasury_change = -5 }
			modify_treasury_effect = yes
			custom_effect_tooltip = FRA_intelligence_surge_tt
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove FRA_intelligence_surge"
			clr_country_flag = FRA_intelligence_surge
		}

		ai_will_do = {
			factor = 15
			modifier = {
				add = 20
				has_country_flag = FRA_terror_level_2
			}
			modifier = {
				add = 30
				has_country_flag = FRA_terror_level_3
			}
		}
	}

	# Secure critical infrastructure
	FRA_secure_critical_infrastructure = {
		icon = GFX_decision_generic_construction

		allowed = { original_tag = FRA }

		available = {
			NOT = { has_country_flag = FRA_infrastructure_secured }
		}

		visible = {
			OR = {
				has_country_flag = FRA_terror_level_2
				has_country_flag = FRA_terror_level_3
				has_idea = FRA_terror_threat_critical
			}
		}

		cost = 100
		days_remove = 270

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision FRA_secure_critical_infrastructure"
			set_temp_variable = { treasury_change = -10 }
			modify_treasury_effect = yes
			custom_effect_tooltip = FRA_securing_infrastructure_tt
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove FRA_secure_critical_infrastructure"
			set_country_flag = FRA_infrastructure_secured
			add_stability = 0.02
			custom_effect_tooltip = FRA_infrastructure_secured_tt
		}

		ai_will_do = {
			factor = 10
			modifier = {
				add = 30
				has_country_flag = FRA_terror_level_3
			}
		}
	}

	# Deploy military for domestic security (Sentinelle)
	FRA_operation_sentinelle = {
		icon = GFX_decision_generic_military_buildup

		allowed = { original_tag = FRA }

		available = {
			NOT = { has_country_flag = FRA_sentinelle_active }
			has_manpower > 10000
		}

		visible = {
			OR = {
				has_country_flag = FRA_terror_level_1
				has_country_flag = FRA_terror_level_2
				has_country_flag = FRA_terror_level_3
				has_idea = FRA_heightened_security
				has_idea = FRA_state_of_emergency_terrorism
			}
		}

		cost = 50
		days_remove = 365

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision FRA_operation_sentinelle"
			set_country_flag = FRA_sentinelle_active
			add_manpower = -7000
			custom_effect_tooltip = FRA_sentinelle_deployed_tt
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove FRA_operation_sentinelle"
			clr_country_flag = FRA_sentinelle_active
			add_manpower = 7000
		}

		ai_will_do = {
			factor = 10
			modifier = {
				add = 15
				has_country_flag = FRA_terror_level_2
			}
			modifier = {
				add = 25
				has_country_flag = FRA_terror_level_3
			}
		}
	}

	# Conduct anti-terror raid
	FRA_anti_terror_raid = {
		icon = GFX_decision_generic_breakthrough

		allowed = { original_tag = FRA }

		available = {
			OR = {
				has_idea = FRA_heightened_security
				has_idea = FRA_state_of_emergency_terrorism
			}
			has_country_flag = FRA_intelligence_surge
		}

		visible = {
			OR = {
				has_country_flag = FRA_terror_level_1
				has_country_flag = FRA_terror_level_2
				has_country_flag = FRA_terror_level_3
			}
		}

		cost = 25
		days_remove = 30
		days_re_enable = 90

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision FRA_anti_terror_raid"
			custom_effect_tooltip = FRA_conducting_raid_tt
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove FRA_anti_terror_raid"
			random_list = {
				70 = {
					# Success
					add_stability = 0.02
					add_political_power = 25
					custom_effect_tooltip = FRA_raid_success_tt
					if = {
						limit = { has_country_flag = FRA_terror_level_3 }
						clr_country_flag = FRA_terror_level_3
						set_country_flag = FRA_terror_level_2
					}
					else_if = {
						limit = { has_country_flag = FRA_terror_level_2 }
						clr_country_flag = FRA_terror_level_2
						set_country_flag = FRA_terror_level_1
					}
					else_if = {
						limit = { has_country_flag = FRA_terror_level_1 }
						clr_country_flag = FRA_terror_level_1
					}
				}
				30 = {
					# Failure
					add_stability = -0.01
					custom_effect_tooltip = FRA_raid_failure_tt
				}
			}
		}

		ai_will_do = {
			factor = 20
			modifier = {
				add = 20
				has_country_flag = FRA_terror_level_2
			}
			modifier = {
				add = 40
				has_country_flag = FRA_terror_level_3
			}
		}
	}

	# Clear terror flags after long period of stability
	FRA_terrorism_defeated = {
		icon = GFX_decision_generic_peaceful_annexation

		allowed = { original_tag = FRA }

		available = {
			NOT = { has_country_flag = FRA_recent_terror_attack }
			NOT = { has_idea = FRA_terror_threat_elevated }
			NOT = { has_idea = FRA_terror_threat_critical }
			has_stability > 0.50
		}

		visible = {
			OR = {
				has_country_flag = FRA_terror_level_1
				has_country_flag = FRA_terror_level_2
				has_country_flag = FRA_terror_level_3
			}
			NOT = { has_country_flag = FRA_recent_terror_attack }
		}

		cost = 50

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision FRA_terrorism_defeated"
			clr_country_flag = FRA_terror_level_1
			clr_country_flag = FRA_terror_level_2
			clr_country_flag = FRA_terror_level_3
			add_stability = 0.05
			add_political_power = 100
			custom_effect_tooltip = FRA_terrorism_defeated_tt
		}

		ai_will_do = {
			factor = 50
		}
	}

	# Rebuild after major attack
	FRA_rebuild_after_attack = {
		icon = GFX_decision_generic_construction

		allowed = { original_tag = FRA }

		available = {
			has_idea = FRA_damaged_infrastructure
		}

		visible = {
			has_idea = FRA_damaged_infrastructure
		}

		cost = 75
		days_remove = 180

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision FRA_rebuild_after_attack"
			set_temp_variable = { treasury_change = -15 }
			modify_treasury_effect = yes
			custom_effect_tooltip = FRA_rebuilding_infrastructure_tt
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove FRA_rebuild_after_attack"
			remove_ideas = FRA_damaged_infrastructure
			add_stability = 0.03
			custom_effect_tooltip = FRA_infrastructure_rebuilt_tt
		}

		ai_will_do = {
			factor = 30
		}
	}

	# Boost tourism after crisis
	FRA_tourism_recovery_campaign = {
		icon = GFX_decision_generic_political_discourse

		allowed = { original_tag = FRA }

		available = {
			has_idea = FRA_tourism_crisis
		}

		visible = {
			has_idea = FRA_tourism_crisis
		}

		cost = 50
		days_remove = 365

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision FRA_tourism_recovery_campaign"
			set_temp_variable = { treasury_change = -8 }
			modify_treasury_effect = yes
			custom_effect_tooltip = FRA_tourism_campaign_tt
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove FRA_tourism_recovery_campaign"
			remove_ideas = FRA_tourism_crisis
			add_stability = 0.02
			custom_effect_tooltip = FRA_tourism_recovered_tt
		}

		ai_will_do = {
			factor = 20
		}
	}

	# Dismantle terror networks - breaks the terror wave
	FRA_dismantle_terror_networks = {
		icon = GFX_decision_generic_break_treaty

		allowed = { original_tag = FRA }

		available = {
			FRA_has_strong_intelligence = yes
			FRA_has_high_police_budget = yes
			has_stability > 0.35
			NOT = { has_country_flag = FRA_recent_terror_attack }
		}

		visible = {
			has_country_flag = FRA_terror_wave_active
		}

		cost = 150
		days_remove = 365

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision FRA_dismantle_terror_networks started"
			set_temp_variable = { treasury_change = -15 }
			modify_treasury_effect = yes
			custom_effect_tooltip = FRA_dismantling_networks_tt
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision FRA_dismantle_terror_networks completed"
			set_country_flag = FRA_terror_networks_dismantled
			clr_country_flag = FRA_terror_wave_active
			add_stability = 0.05
			add_political_power = 75
			custom_effect_tooltip = FRA_terror_networks_dismantled_tt
		}

		ai_will_do = {
			factor = 100
			modifier = {
				factor = 0
				NOT = { has_country_flag = FRA_terror_wave_active }
			}
		}
	}
}