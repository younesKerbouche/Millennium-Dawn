resource_storage_category = {
	test_decision_rss = {
        icon = GFX_decision_generic_infrastructure
        allowed = { always = no }
        available = { always = yes }

        days_mission_timeout = 3
        fire_only_once = no
        selectable_mission = yes

        timeout_effect = {
            add_to_variable = { max_resource_storage = 150 }
        }
    }


    take_from_storage = {
        icon = GFX_decision_generic_infrastructure
        allowed = { always = yes }
        visible = {
            is_debug = yes
        }
        fire_only_once = no

        complete_effect = {
            set_country_flag = allowed_to_take
            add_dynamic_modifier = {
                modifier = resource_storage_modifier
            }
        }
    }
    do_not_take_from_storage = {
        icon = GFX_decision_generic_infrastructure
        allowed = { always = yes }
        visible = {
            is_debug = yes
        }
        fire_only_once = no

        complete_effect = {
            clr_country_flag = allowed_to_take
            remove_dynamic_modifier = {
                modifier = resource_storage_modifier
            }

        }
    }
}
