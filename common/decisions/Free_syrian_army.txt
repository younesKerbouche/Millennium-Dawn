####FREE SYRIAN ARMY####
FSA_internal_revolt_category = {

	FSA_defection_to_extremist = {

		allowed = { tag = FSA }

		icon = GFX_decision_revolt

		available = {
			hidden_trigger = { always = no }
		}

		war_with_on_timeout = NUS
	
		days_mission_timeout = 140
		is_good = yes
		fire_only_once = yes

		cancel_trigger = {
			hidden_trigger = {
				OR = {
					has_government = fascism
					has_country_flag = fought_extremist_flag
				}
			}
		}

		activation = {
			has_global_flag = FSA_free_syrian_army_active_flag
			NOT = {
				has_government = fascism
				has_country_flag = fought_extremist_flag
			}
		}

		visible = {
			NOT = {
				has_government = fascism
				has_country_flag = fought_extremist_flag
			}
		}

		cancel_effect = {

		}

		timeout_effect = {
			activate_mission = FSA_defection_to_extremist
		}

		ai_will_do = {
			factor = 200
		}
	} 

	FSA_capture_aleppo = {

		allowed = { tag = FSA }

		icon = GFX_decision_generic_operation

		available = {
			tag = FSA
			controls_state = 566
			controls_province = 12473
		}

		days_mission_timeout = 120
		is_good = no
		fire_only_once = yes

		cancel_trigger = {
			hidden_trigger = {
				OR = {
					has_government = fascism
					has_country_flag = fought_extremist_flag
				}
			}
		}

		activation = {
			tag = FSA
			has_global_flag = FSA_free_syrian_army_active_flag
			NOT = {
				has_country_flag = FSA_capture_aleppo
				OR = {
					has_government = fascism
					has_country_flag = fought_extremist_flag
				}
			}
		}

		visible = {
			tag = FSA
			NOT = {
				OR = {
					has_government = fascism
					has_country_flag = fought_extremist_flag
				}
			}
		}

		timeout_effect = { set_country_flag = FSA_capture_aleppo }

		complete_effect = {
			set_country_flag = FSA_capture_aleppo
			add_days_mission_timeout = {
				mission = FSA_defection_to_extremist
				days = 200
			}
		}

		ai_will_do = {
			base = 200
		}
	}

	FSA_capture_ar_raqqah = {

		allowed = { tag = FSA }

		icon = GFX_decision_generic_operation

		available = {
			tag = FSA
			controls_state = 191
			controls_province = 3127
		}

		days_mission_timeout = 500
		is_good = no
		fire_only_once = yes

		cancel_trigger = {
			hidden_trigger = {
				OR = {
					has_government = fascism
					has_country_flag = fought_extremist_flag
				}
			}
		}

		activation = {
			tag = FSA
			has_global_flag = FSA_free_syrian_army_active_flag
			NOT = {
				has_country_flag = FSA_capture_ar_raqqah
				OR = {
					has_government = fascism
					has_country_flag = fought_extremist_flag
				}
			}
		}

		visible = {
			tag = FSA
			NOT = {
				OR = {
					has_government = fascism
					has_country_flag = fought_extremist_flag
				}
			}
		}

		timeout_effect = { set_country_flag = FSA_capture_ar_raqqah }

		complete_effect = {
			set_country_flag = FSA_capture_ar_raqqah
			add_days_mission_timeout = {
				mission = FSA_defection_to_extremist
				days = 200
			}
		}

		ai_will_do = {
			base = 200
		}
	}

	FSA_capture_damascus = {

		allowed = { tag = FSA }

		icon = GFX_decision_generic_operation

		available = {
			tag = FSA
			controls_state = 186
			controls_province = 4111
		}

		days_mission_timeout = 600
		is_good = no
		fire_only_once = yes

		cancel_trigger = {
			hidden_trigger = {
				OR = {
					has_government = fascism
					has_country_flag = fought_extremist_flag
				}
			}
		}

		activation = {
			tag = FSA
			has_global_flag = FSA_free_syrian_army_active_flag
			NOT = {
				has_country_flag = FSA_capture_damascus
				OR = {
					has_government = fascism
					has_country_flag = fought_extremist_flag
				}
			}
		}

		visible = {
			tag = FSA
			NOT = {
				OR = {
					has_government = fascism
					has_country_flag = fought_extremist_flag
				}
			}
		}

		timeout_effect = { set_country_flag = FSA_capture_damascus }

		complete_effect = {
			set_country_flag = FSA_capture_damascus
			add_days_mission_timeout = {
				mission = FSA_defection_to_extremist
				days = 200
			}
		}

		ai_will_do = {
			base = 200
		}
	}

	FSA_capture_deir_ez_zur = {

		allowed = { tag = FSA }

		icon = GFX_decision_generic_operation

		available = {
			tag = FSA
			controls_state = 192
			controls_province = 6312
		}

		days_mission_timeout = 550
		is_good = no
		fire_only_once = yes

		cancel_trigger = {
			hidden_trigger = {
				OR = {
					has_government = fascism
					has_country_flag = fought_extremist_flag
				}
			}
		}

		activation = {
			tag = FSA
			has_global_flag = FSA_free_syrian_army_active_flag
			NOT = {
				has_country_flag = FSA_capture_deir_ez_zur
				OR = {
					has_government = fascism
					has_country_flag = fought_extremist_flag
				}
			}
		}

		visible = {
			tag = FSA
			NOT = {
				OR = {
					has_government = fascism
					has_country_flag = fought_extremist_flag
				}
			}
		}

		timeout_effect = { set_country_flag = FSA_capture_deir_ez_zur }

		complete_effect = {
			set_country_flag = FSA_capture_deir_ez_zur
			add_days_mission_timeout = {
				mission = FSA_defection_to_extremist
				days = 200
			}
		}

		ai_will_do = {
			base = 200
		}
	}

	FSA_capture_hama = {

		allowed = { tag = FSA }

		icon = GFX_decision_generic_operation

		available = {
			tag = FSA
			controls_state = 571
			controls_province = 14599
		}

		days_mission_timeout = 410
		is_good = no
		fire_only_once = yes

		cancel_trigger = {
			hidden_trigger = {
				OR = {
					has_government = fascism
					has_country_flag = fought_extremist_flag
				}
			}
		}

		activation = {
			tag = FSA
			has_global_flag = FSA_free_syrian_army_active_flag
			NOT = {
				has_country_flag = FSA_capture_hama
				OR = {
					has_government = fascism
					has_country_flag = fought_extremist_flag
				}
			}
		}

		visible = {
			tag = FSA
			NOT = {
				OR = {
					has_government = fascism
					has_country_flag = fought_extremist_flag
				}
			}
		}

		timeout_effect = { set_country_flag = FSA_capture_hama }

		complete_effect = {
			set_country_flag = FSA_capture_hama
			add_days_mission_timeout = {
				mission = FSA_defection_to_extremist
				days = 200
			}
		}

		ai_will_do = {
			base = 200
		}
	}

	FSA_capture_homs = {

		allowed = { tag = FSA }

		icon = GFX_decision_generic_operation

		available = {
			tag = FSA
			controls_state = 188
			controls_province = 6302
		}

		days_mission_timeout = 440
		is_good = no
		fire_only_once = yes

		cancel_trigger = {
			hidden_trigger = {
				OR = {
					has_government = fascism
					has_country_flag = fought_extremist_flag
				}
			}
		}

		activation = {
			tag = FSA
			has_global_flag = FSA_free_syrian_army_active_flag
			NOT = {
				has_country_flag = FSA_capture_homs
				OR = {
					has_government = fascism
					has_country_flag = fought_extremist_flag
				}
			}
		}

		visible = {
			tag = FSA
			NOT = {
				OR = {
					has_government = fascism
					has_country_flag = fought_extremist_flag
				}
			}
		}

		timeout_effect = { set_country_flag = FSA_capture_homs }

		complete_effect = {
			set_country_flag = FSA_capture_homs
			add_days_mission_timeout = {
				mission = FSA_defection_to_extremist
				days = 200
			}
		}

		ai_will_do = {
			base = 200
		}
	}

	FSA_capture_latakia = {

		allowed = { tag = FSA }

		icon = GFX_decision_generic_operation

		available = {
			tag = FSA
			controls_state = 189
			controls_province = 1088
		}

		days_mission_timeout = 900
		is_good = no
		fire_only_once = yes

		cancel_trigger = {
			hidden_trigger = {
				OR = {
					has_government = fascism
					has_country_flag = fought_extremist_flag
				}
			}
		}

		activation = {
			tag = FSA
			has_global_flag = FSA_free_syrian_army_active_flag
			NOT = {
				has_country_flag = FSA_capture_latakia
				OR = {
					has_government = fascism
					has_country_flag = fought_extremist_flag
				}
			}
		}

		visible = {
			tag = FSA
			NOT = {
				OR = {
					has_government = fascism
					has_country_flag = fought_extremist_flag
				}
			}
		}
		
		timeout_effect = { set_country_flag = FSA_capture_latakia }

		complete_effect = {
			set_country_flag = FSA_capture_latakia
			add_days_mission_timeout = {
				mission = FSA_defection_to_extremist
				days = 200
			}
		}

		ai_will_do = {
			base = 200
		}
	}

	FSA_capture_tartus = {

		allowed = { tag = FSA }

		icon = GFX_decision_generic_operation

		available = {
			tag = FSA
			controls_state = 189
			controls_province = 10087
		}

		days_mission_timeout = 950
		is_good = no
		fire_only_once = yes

		cancel_trigger = {
			hidden_trigger = {
				OR = {
					has_government = fascism
					has_country_flag = fought_extremist_flag
				}
			}
		}

		activation = {
			tag = FSA
			has_global_flag = FSA_free_syrian_army_active_flag
			NOT = {
				has_country_flag = FSA_capture_tartus
				OR = {
					has_government = fascism
					has_country_flag = fought_extremist_flag
				}
			}
		}

		visible = {
			tag = FSA
			NOT = {
				OR = {
					has_government = fascism
					has_country_flag = fought_extremist_flag
				}
			}
		}

		timeout_effect = { set_country_flag = FSA_capture_tartus }

		complete_effect = {
			set_country_flag = FSA_capture_tartus
			add_days_mission_timeout = {
				mission = FSA_defection_to_extremist
				days = 200
			}
		}

		ai_will_do = {
			base = 200
		}
	}	
}