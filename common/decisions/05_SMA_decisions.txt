SMA_research_center_category = {
	SMA_build_i = {
		icon = GFX_decision_generic_operation
		cost = 50
		days_remove = 50

		visible = {
			NOT = {
				has_idea = SMA_center
			}
		}

		available = {
			NOT = {
				OR = {
					has_idea = SMA_center_i
					has_idea = SMA_center_ii
					has_idea = SMA_center_iii
					has_idea = SMA_center
				}
			}
		}

		remove_effect = {
			add_ideas = {
				SMA_center_i
			}
			set_temp_variable = { treasury_change = -1.00 }
			modify_treasury_effect = yes
		}
	}
	SMA_build_ii = {
		icon = GFX_decision_generic_operation
		cost = 50
		days_remove = 100

		visible = {
			NOT = {
				has_idea = SMA_center
			}
		}

		available = {
			NOT = {
				OR = {
					has_idea = SMA_center_ii
					has_idea = SMA_center_iii
					has_idea = SMA_center
				}
			}
			has_idea = SMA_center_i
		}

		remove_effect = {
			swap_ideas = {
				remove_idea = SMA_center_i
				add_idea = SMA_center_ii
			}
			set_temp_variable = { treasury_change = -1.00 }
			modify_treasury_effect = yes
		}
	}
	SMA_build_iii = {
		icon = GFX_decision_generic_operation
		cost = 50
		days_remove = 100

		visible = {
			NOT = {
				has_idea = SMA_center
			}
		}

		available = {
			NOT = {
				OR = {
					has_idea = SMA_center_iii
					has_idea = SMA_center
				}
			}
			has_idea = SMA_center_ii
		}

		remove_effect = {
			swap_ideas = {
				remove_idea = SMA_center_ii
				add_idea = SMA_center_iii
			}
			set_temp_variable = { treasury_change = -1.00 }
			modify_treasury_effect = yes
		}
	}
	SMA_build_final = {
		icon = GFX_decision_generic_operation
		cost = 50
		days_remove = 50

		visible = {
			NOT = {
				has_idea = SMA_center
			}
		}

		available = {
			NOT = {
				OR = {
					has_idea = SMA_center
				}
			}
			has_idea = SMA_center_iii
		}

		remove_effect = {
			swap_ideas = {
				remove_idea = SMA_center_iii
				add_idea = SMA_center
			}
			set_temp_variable = { treasury_change = -1.00 }
			modify_treasury_effect = yes
		}
	}
}

SMA_change_textbooks = {
	SMA_change_language = {
		icon = GFX_decision_ger_mefo_bills

		visible = {
			NOT = {
				OR = {
					has_country_flag = change_texbooks_lang_romanian
					has_country_flag = change_texbooks_lang_ita
				}
			}
		}

		complete_effect = {
			if = {
				limit = {
					has_country_flag = change_texbooks_lang
				}
				clr_country_flag = change_texbooks_lang
			}
			else = {
				set_country_flag = change_texbooks_lang
			}
		}
	}
	SMA_change_language_ita = {
		icon = GFX_decision_generic_operation
		cost = 50
		days_remove = 30

		available = {
			NOT = {
				has_decision = SMA_change_language_romanian
			}
		}

		visible = {
			has_country_flag = change_texbooks_lang
			NOT = {
				OR = {
					has_country_flag = change_texbooks_lang_romanian
					has_country_flag = change_texbooks_lang_ita
				}
			}
		}

		remove_effect = {
			set_temp_variable = { treasury_change = -1.00 }
			modify_treasury_effect = yes
			add_ideas = SMA_lang_ita
			clr_country_flag = change_texbooks_lang
			set_country_flag = change_texbooks_lang_ita
		}
	}
	SMA_change_language_romanian = {
		icon = GFX_decision_generic_operation
		cost = 50
		days_remove = 30

		available = {
			NOT = {
				has_decision = SMA_change_language_ita
			}
		}

		visible = {
			has_country_flag = change_texbooks_lang
			NOT = {
				OR = {
					has_country_flag = change_texbooks_lang_romanian
					has_country_flag = change_texbooks_lang_ita
				}
			}
		}

		remove_effect = {
			set_temp_variable = { treasury_change = -1.00 }
			modify_treasury_effect = yes
			add_ideas = SMA_lang_romanion
			clr_country_flag = change_texbooks_lang
			set_country_flag = change_texbooks_lang_romanian
		}
	}

	SMA_change_history = {
		icon = GFX_decision_ger_mefo_bills

		visible = {
			NOT = {
				OR = {
					has_country_flag = change_texbooks_hist_peace
					has_country_flag = change_texbooks_hist_mil
				}
			}
		}

		complete_effect = {
			if = {
				limit = {
					has_country_flag = change_texbooks_hist
				}
				clr_country_flag = change_texbooks_hist
			}
			else = {
				set_country_flag = change_texbooks_hist
			}
		}
	}
	SMA_change_hist_peace = {
		icon = GFX_decision_generic_operation
		cost = 50
		days_remove = 30

		available = {
			NOT = {
				has_decision = SMA_change_hist_mil
			}
		}

		visible = {
			has_country_flag = change_texbooks_hist
			NOT = {
				OR = {
					has_country_flag = change_texbooks_hist_peace
					has_country_flag = change_texbooks_hist_mil
				}
			}
		}

		remove_effect = {
			set_temp_variable = { treasury_change = -1.00 }
			modify_treasury_effect = yes
			add_ideas = SMA_hist_peace
			clr_country_flag = change_texbooks_hist
			set_country_flag = change_texbooks_hist_peace
		}
	}
	SMA_change_hist_mil = {
		icon = GFX_decision_generic_operation
		cost = 50
		days_remove = 30

		available = {
			NOT = {
				has_decision = SMA_change_hist_peace
			}
		}

		visible = {
			SMA = {
				has_country_flag = change_texbooks_hist
				NOT = {
					OR = {
						has_country_flag = change_texbooks_hist_peace
						has_country_flag = change_texbooks_lang_mil
					}
				}
			}
		}

		remove_effect = {
			set_temp_variable = { treasury_change = -1.00 }
			modify_treasury_effect = yes
			add_ideas = SMA_hist_mil
			clr_country_flag = change_texbooks_hist
			set_country_flag = change_texbooks_hist_mil
		}
	}

	##### ED ##### ED ##### ED ##### ED ##### ED ##### ED ##### ED ##### ED #####
	SMA_change_education = {
		icon = GFX_decision_ger_mefo_bills

		visible = {
			NOT = {
				OR = {
					has_country_flag = change_texbooks_ed_work
					has_country_flag = change_texbooks_ed_res
					has_country_flag = change_texbooks_ed_mil
				}
			}
		}

		complete_effect = {
			if = {
				limit = {
					SMA = {
						has_country_flag = change_texbooks_ed
					}
				}
				SMA = {
					clr_country_flag = change_texbooks_ed
				}
			}
			else = {
				if = {
					limit = { SMA = { NOT = { has_country_flag = change_texbooks_ed } } }
					SMA = {
						set_country_flag = change_texbooks_ed
					}
				}
			}
		}
	}
	SMA_change_ed_work = {
		icon = GFX_decision_generic_operation
		cost = 50
		days_remove = 30

		available = {
			SMA = {
				NOT = {
					OR = {
						has_decision = SMA_change_ed_res
						has_decision = SMA_change_ed_mil
					}
				}
			}
		}

		visible = {
			SMA = {
				has_country_flag = change_texbooks_ed
				NOT = {
					OR = {
						has_country_flag = change_texbooks_ed_work
						has_country_flag = change_texbooks_ed_res
						has_country_flag = change_texbooks_ed_mil
					}
				}
			}
		}

		remove_effect = {
			set_temp_variable = { treasury_change = -1.00 }
			modify_treasury_effect = yes
			SMA = {
				add_ideas = SMA_ed_work
				clr_country_flag = change_texbooks_ed
				set_country_flag = change_texbooks_ed_work
			}
		}
	}
	SMA_change_ed_res = {
		icon = GFX_decision_generic_operation
		cost = 50
		days_remove = 30

		available = {
			SMA = {
				NOT = {
					OR = {
						has_decision = SMA_change_ed_work
						has_decision = SMA_change_ed_mil
					}
				}
			}
		}

		visible = {
			SMA = {
				has_country_flag = change_texbooks_ed
				NOT = {
					OR = {
						has_country_flag = change_texbooks_ed_work
						has_country_flag = change_texbooks_ed_res
						has_country_flag = change_texbooks_ed_mil
					}
				}
			}
		}

		remove_effect = {
			set_temp_variable = { treasury_change = -1.00 }
			modify_treasury_effect = yes
			SMA = {
				add_ideas = SMA_ed_res
				clr_country_flag = change_texbooks_ed
				set_country_flag = change_texbooks_ed_res
			}
		}
	}
	SMA_change_ed_mil = {
		icon = GFX_decision_generic_operation
		cost = 50
		days_remove = 30

		available = {
			SMA = {
				NOT = {
					OR = {
						has_decision = SMA_change_ed_work
						has_decision = SMA_change_ed_res
					}
				}
			}
		}

		visible = {
			SMA = {
				has_country_flag = change_texbooks_ed
				NOT = {
					OR = {
						has_country_flag = change_texbooks_ed_work
						has_country_flag = change_texbooks_ed_res
						has_country_flag = change_texbooks_ed_mil
					}
				}
			}
		}

		remove_effect = {
			set_temp_variable = { treasury_change = -1.00 }
			modify_treasury_effect = yes
			SMA = {
				add_ideas = SMA_ed_mil
				clr_country_flag = change_texbooks_ed
				set_country_flag = change_texbooks_ed_mil
			}
		}
	}
}