add_namespace = test
add_namespace = debug_money
add_namespace = systems

# INFO: Add debug events here for ease checking of content or code

country_event = {
	id = systems.1
	title = systems.1.t
	desc = systems.1.d
	is_triggered_only = yes

	option = {
		name = systems.1.a
	}
}
